# Audit del sito — 23 agosto 2026

Cinque giri nella stessa giornata: il **guscio del sito**, poi **Analisi di
bilancio** a fondo, poi **gli altri cinque tool**, poi le **pagine editoriali**,
poi i **PDF**. I cinque verbali sono in fila qui sotto.

## Giro 1 — il guscio

Perimetro del giro: il **guscio del sito** (66 pagine HTML), il **catalogo dei
tool**, i **documenti legali**, le **librerie incorporate** e una lettura del
**testo che l'utente legge**. I sei tool sono stati aperti e navigati; l'esame
funzionale voce per voce è rimandato al giro 2.

Base di partenza, verificata prima di toccare qualsiasi cosa: 83 test node
superati, 42 controlli responsive superati, contrasto senza fallimenti.
Alla fine del giro: **90 test node, 42 responsive, contrasto pulito.**

---

## 1. La libreria incorporata era stata sovrascritta dal codice del tool

È il difetto più serio trovato, e non era nell'elenco di nessuno.

In `tools/financial-analysis/index.html`, **dentro** il bundle minificato di
SheetJS, al posto della funzione `tr` della libreria c'era questo:

```js
var er;function rr(e){er=e}function talReclassRow(label,c,p,cls=''){
  const hasPrev=!!(STATE.periods&&STATE.periods.previous&&STATE.periods.previous.available);
  ...
}function ar(e,r,t){...}
```

`talReclassRow` è codice del tool — legge `STATE.periods`, chiama `esc`, `money`,
`pct`. Una sostituzione larga su un file da 1,7 MB era andata a segno nel punto
sbagliato, mangiandosi `function tr(e)`, che nel file non esisteva più.

**Conseguenza vera, riproducibile.** La versione di `talReclassRow` finita nel
bundle era quella **buona**: gestisce il caso in cui non c'è l'esercizio
precedente. La versione che girava era la vecchia, senza quella guardia. Misurato
in pagina, senza esercizio precedente:

| Voce | Corrente | Precedente | Δ | Δ% |
|---|---|---|---|---|
| Attivo immobilizzato | 150.000 € | — | **150.000 €** | — |

La colonna della variazione mostrava l'intero saldo corrente come se fosse una
crescita, con la colonna del precedente vuota. Chi legge vede
«immobilizzazioni +150.000» dove non c'è alcun termine di paragone.

Che `tr` fosse sparita non rompeva nulla — non viene mai chiamata dal ramo
browser — ma era una mina: al primo aggiornamento della libreria o al primo
percorso di codice che la usa, il tool si sarebbe rotto in silenzio.

**Corretto:** la definizione viva di `talReclassRow` è ora quella con la
guardia. Verificato in pagina: senza esercizio precedente le tre colonne di
confronto dicono «—»; con l'esercizio precedente dicono
`150.000 € · 120.000 € · 30.000 € · 25,0%`.

## 2. La firma di SheetJS valeva per un tool su sei

`THIRD-PARTY-NOTICES.txt` pubblicava una sola firma SHA-256 per la copia di
SheetJS «embedded in each operational tool HTML». Misurata: corrispondeva **solo
al Generatore F24**. Normalizzando i fine riga, i sei tool incorporavano
**quattro sequenze di byte diverse**:

| Tool | Copia |
|---|---|
| f24 | quella dichiarata |
| lipe, tfa-client-file, confronto-regimi | identiche fra loro, 6 byte in meno |
| financial-statement | come sopra, più la sotto-libreria `codepage` a 1.3.4 invece di 1.2.0 |
| financial-analysis | quella col codice del tool dentro |

I 6 byte mancanti sono **due caratteri a larghezza zero** (U+200C e U+200D) in
una tabella di codepage: la firma di un testo passato attraverso qualcosa che
normalizza i caratteri invisibili, non di una versione diversa della libreria.

**Corretto:** i sei tool incorporano ora la stessa identica copia, quella che
corrisponde alla firma pubblicata. Verificato in pagina su tutti e sei che
SheetJS 0.20.3 legge e scrive: `XLSX.write` → `XLSX.read` con accenti italiani
torna identico, e i sei producono lo stesso file di 15.953 byte.

## 3. Un test nuovo, che questa classe di difetti non la fa più passare

`tests/vendored-libraries.test.mjs`, quattro controlli:

1. i sei tool incorporano la stessa identica copia di SheetJS;
2. quella copia è la firma dichiarata nei notices;
3. **nel bundle non c'è codice del tool** (funzioni `tal*`, letture di `STATE`);
4. il tag `<script>` dichiara `data-vendored="SheetJS 0.20.3"`.

Provato che falliscono davvero: rimettendo il file di prima, tutti e quattro
diventano rossi con il messaggio giusto.

## 4. Le versioni nei tre cataloghi erano ferme, e nessun test le guardava

`tools/manifest.json` è l'elenco autorevole e il test esistente lo confronta con
le pagine dei tool. Ma il **badge che l'utente legge** sta nelle pagine di
catalogo, fuori da ogni controllo:

| Tool | Manifest | `/tools/` | `/en/tools/` e `/es/tools/` |
|---|---|---|---|
| Bilancio civilistico | 1.4.1 | 1.4.0 | **1.3.6** |
| Analisi di bilancio | 2.1.0 | 2.1.0 | **2.0.6** |
| Generatore LIPE | 3.6.1 | 3.5.0 | **3.4.3** |
| Fascicolo, F24, Confronto regimi | — | corretti | corretti |

Corretti tutti e tre. E `tests/version-coherence.test.mjs` ora legge le sei
schede di ciascun catalogo, ne estrae badge e `slug`, e li confronta col
manifest — anche questo verificato che fallisce quando deve.

## 5. «Configura con AI»: gli accenti scritti con l'apostrofo

La pagina mescolava le due grafie **nello stesso paragrafo**: «cioè» e «ciò»
accanto a «piu'», «e'», «c'e'». Non era una scelta: i tre prompt più vecchi
(Bilancio, Analisi, LIPE) erano quasi corretti, i tre più recenti (Fascicolo,
F24, Confronto regimi) quasi interamente in apostrofo. La pagina spagnola
equivalente non ha un solo apostrofo al posto di un accento.

- `configura-con-ai/index.html`: **160 correzioni**
- `resources/templates/Prompt_F24_AI_v1.0.txt`: 39
- `resources/templates/Prompt_Fascicolo_Fiscale_Cliente_AI_v1.0.txt`: 36

Comprese le maiuscole (`SOCIETA'` → `SOCIETÀ`) e le elisioni composte (`c'e'` →
`c'è`, `com'e'` → `com'è`, `dell'entita'` → `dell'entità`). Restano intatti gli
apostrofi veri (`all'`, `un po'`, `dell'`).

Il test `prompt F24: il .txt scaricabile coincide con quello della pagina` ha
fatto il suo mestiere: ha bocciato la correzione fatta a metà, quando avevo
sistemato la pagina e non ancora il file scaricabile.

## 6. Otto pulsanti «chiudi» senza nome accessibile

Nello stesso tool convivevano `<button class="close-btn" aria-label="Chiudi">` e
`<button class="close-btn">`, entrambi con la sola crocetta `×`: il secondo, con
lo screen reader, si annuncia «times». Aggiunto `aria-label="Chiudi"` ai sette
del Bilancio e all'uno di Analisi.

## 7. Documenti legali e testo residuo

- `THIRD-PARTY-NOTICES.txt` diceva «The **five** operational tools» e la tabella
  per tool non elencava Confronto regimi. Aggiunta la riga, aggiornata la data,
  e aggiunta la nota su cosa è successo alle copie di SheetJS.
- `tools/convenienza-fiscale/index.html`: «Il tool **e'** lo stesso» → «è».

---

## Quello che ho verificato e NON era un difetto

Vale la pena scriverlo: sono le trappole in cui si ricasca.

- **Le versioni sulle tre home** (`v1.3.4`, `v3.4.1`) sembrano vecchissime a
  `grep`, ma **non sono rese**: stanno in un dizionario di traduzione che non ha
  più elementi `[data-i18n]` a cui applicarsi. Misurato in pagina: zero
  `[data-i18n]`, zero `[data-lang]`.
- **`tools/tfa-client-file`** sembra avere una sidebar mutilata: la prima
  `.workspace-nav` ha tre voci. Ne ha **cinque**, di `.workspace-nav`, ed è
  completa.
- **LIPE è allineato al concept**, contro quello che diceva questo repository:
  ha «Dati e backup», «Come si usa» e i deep link (`#/clienti` →
  `#/quadro-vp`, con Indietro e Avanti che camminano fra le sezioni). Punto
  chiuso in `CONTESTO.md`.
- **I sei tool non fanno una sola richiesta fuori origine.**
- **Nessuno scorrimento orizzontale** e comando del menu 44×44 corretto su
  6 tool × 7 larghezze. Nessun testo sotto il rapporto di contrasto 3,0.

---

## Deciso in questo giro

- **Il percorso numerato non è più un'eccezione dell'F24: è una regola.** I tool
  che preparano qualcosa con un ordine vincolante usano `PERCORSO` a passi
  numerati; gli altri la sidebar `Configurazione / Lavoro / Risultati e dati`.
  Confronto regimi resta com'è. Scritto in `CONTESTO.md`.

## Aperto — messo in coda per il giro 2

Tutti e quattro decisi da Riccardo il 23 agosto, da fare nel prossimo giro:

1. **Uniformare i `?v=` degli asset.** Oggi la regola vale davvero solo per
   `tal-design.css`:

   | Asset | Stato |
   |---|---|
   | `tal-design.css` | 61 riferimenti, tutti `?v=20260814` ✔ |
   | `tal-experience.js` | 56 a `?v=20260812`, **4 a `?v=20260813`** |
   | `site-shell.css` | **51 senza `?v=`**, 4 a `20260810-v61510-header-1`, 2 a `20260813`, 1 a `20260811-ctp-230` |
   | `site-shell.js` | 4 riferimenti, nessun `?v=` |

2. **Togliere il codice morto.** Otto pagine portano un dizionario di traduzione
   a tre lingue con la sua `setLang`, e **zero elementi a cui applicarlo**. Nella
   home ci sono anche regole CSS per blocchi che non esistono più
   (`.author-signature`, `.home-privacy-badge`, `.home-quick-links`). È il motivo
   per cui un `grep` sulle versioni dà risposte sbagliate.

3. **`canonical` sulle 12 pagine di avviso.** `/en/tools/<slug>/` e
   `/es/tools/<slug>/` sono le uniche 12 pagine del sito senza `canonical`: va
   fatto puntare alla versione italiana.

4. **Le date «Aggiornato il» del catalogo.** Quattro tool su sei dicono ancora
   «10 agosto 2026». Il Bilancio è stato modificato il 14, LIPE il 14 e dopo,
   Analisi e Fascicolo il 21-22, Confronto regimi il 23. Da allineare e da
   mettere sotto test.

## Aperto — trovato e non ancora deciso

- **Il Bilancio numera le stesse sezioni in due modi.**

  | Sidebar | Intestazione della pagina |
  |---|---|
  | ✎ Anagrafica | **I ·** ANAGRAFICA DELLA SOCIETÀ |
  | ⇩ Importa da Excel | **II ·** IMPORT DEL BILANCIO |
  | **1** Bilancio ITA GAAP | **III ·** BILANCIO ITA GAAP |

  Chi legge «1 Bilancio» nella sidebar arriva su una pagina intitolata «III». E
  l'intestazione dice «IMPORT del bilancio» dove la sidebar dice «Importa».

- **Le icone della stessa azione cambiano da tool a tool.** «Esporta» è `↧` nel
  Bilancio, in LIPE e nel Fascicolo, ma `P` in Analisi. I passi di lavoro sono
  cifre arabe nel Bilancio e in Analisi, numeri romani in LIPE (`I`, `II`).

- **Confronto regimi è l'unico tool che non produce un PDF**: solo Excel. Resta
  aperto anche il punto vecchio su Analisi, che stampa dal browser invece di
  scaricare con jsPDF.

- **La home chiama jsDelivr, la pagina privacy non lo dice.** L'intro importa
  `threejs-components@0.0.19` da `cdn.jsdelivr.net` come modulo dinamico. È
  documentato nei *Third-party notices* (sezione 3) ed è decorativo, con ripiego
  se fallisce. Ma la pagina Privacy elenca i fornitori «tra cui GitHub Pages e
  Cloudflare» e non nomina jsDelivr, che riceve l'IP di ogni visitatore alla
  prima apertura della home. O si nomina, o si ospita il modulo in `assets/`.

---

# Giro 2 — Analisi di bilancio, 23 agosto 2026

Aperte tutte e ventuno le sezioni, premuto ogni pulsante, e **aperti davvero i
file prodotti**: non il codice che li genera, ma i byte che arrivano al browser,
riletti con `XLSX.read`. È la differenza che ha fatto trovare quasi tutto.

Alla fine: 93 test node, 42 controlli responsive, contrasto pulito, zero errori
in console attraversando tutte le sezioni.

## Le prime tre decisioni, chiuse

- **Numerazione.** Tolti i numeri romani dalle intestazioni di Bilancio, LIPE e
  Analisi. Erano tre sistemi incompatibili con le rispettive sidebar: chi
  leggeva «1 Bilancio» arrivava su una pagina intitolata «III», LIPE numerava
  I, III, IV, V (il II non esisteva), Analisi aveva **due** sezioni «III».
  Ora l'ordine lo dice la sidebar e l'intestazione dice il nome, uguale.
- **Icona di «Esporta»** in Analisi: era `P`, ora è `↧` come negli altri tre.
- **jsDelivr** nominato nella pagina Privacy, in tutte e tre le lingue.

## Quello che non si vedeva dal codice

### Il formato dei numeri negli Excel

Tre fogli su dieci del working paper e **tutti e sei** quelli delle analisi
avanzate uscivano senza un formato numerico: un current ratio si leggeva
`1,8873239436619718`, la mappatura conti mostrava gli importi senza separatore.
I sei prospetti riclassificati erano invece formattati bene — lo standard c'era,
mancava applicarlo al resto.

La rifinitura sta in `assets/tal-xlsx-guard.js`, che era già il punto unico da
cui passa la scrittura. La versione precedente indirizzava le celle **per
posizione fissa** (`B7:D12`) e cercava il foglio per nome: bastava una riga in
più perché formattasse le celle sbagliate — ed è quello che è successo appena ho
rinominato il foglio. Ora cerca la riga di intestazione e prende il formato
dall'**unità di misura che i KPI già dichiarano**, che è l'unico modo esatto di
sapere se `0,48` è «48,4%» o «0,48x». Gli anni restano numeri nudi.

### Cinque guardie che non scattano mai

In `financial-analysis` cinque blocchi di correzione sono protetti da
`typeof X === 'function'` dove `X` sta **dentro un altro IIFE**: `typeof`
risponde `'undefined'` e il blocco non viene mai eseguito.

| Nome | Cosa doveva fare | Stato |
|---|---|---|
| `v20DashboardRows` | rinominare «Business Health Score», riscrivere la nota, aggiungere la copertura benchmark | **corretto alla fonte** |
| `v18Write` | messaggio di conferma dopo l'export e guardia contro il doppio clic | **messaggio aggiunto alla fonte** |
| `v18WorkbookLineage` | aggiungere un foglio di tracciabilità dei conti | da decidere: duplicherebbe «Mappatura conti» |
| `v20AddIncidence` | colonne di incidenza a schermo | **non serve**: un'altra strada le mette già |
| `v18RequireExportGate` | gate di export | **non serve**: il gate vivo è `gate()` in `protect()` |

Le prime due erano difetti veri e visibili: la dashboard nell'Excel diceva
ancora «Business Health Score sperimentale» in inglese, e «Working paper Excel»
era **l'unico export del tool che non diceva niente** dopo aver scaricato il
file.

### Cinque nomi per lo stesso backup

Contati sui pulsanti resi: `exportConfig()` si chiamava in **tre** modi diversi a
seconda della schermata, `exportAllConfigs()` in **due**.

| Dove | Prima | Ora |
|---|---|---|
| Anagrafica | Esporta configurazione JSON | Scarica un backup di questa società |
| Dati e backup | Esporta società (JSON) | Scarica un backup di questa società |
| Esporta | Backup JSON società | Scarica un backup di questa società |
| Clienti | Esporta archivio JSON | Scarica un backup dell'archivio |
| Dati e backup | Esporta archivio (JSON) | Scarica un backup dell'archivio |

Adottato il vocabolario di **Confronto regimi**, che è il più recente e il più
leggibile: *si scarica un backup, si ripristina da backup*. Vale come canone per
la suite: gli altri quattro tool hanno ancora il loro (vedi sotto).

### Il gergo

«i gate di qualità», «Gate professionale», «Import transazionale», «Cash
conversion proxy», «Pacchetto di reporting», «Executive dashboard», «report con
lineage», e **«proxy» sedici volte** nel rendiconto finanziario e nella crisi
d'impresa. «Proxy» è diventato «stima»: è quello che è, ed è la parola che un
commercialista userebbe parlando.

Restano dentro i termini che in italiano si usano davvero così: budget,
forecast, benchmark, cash flow, template, business unit.

E i sei pulsanti di export si chiamavano in quattro modi diversi («Esporta KPI
Excel», «KPI Excel», «Esporta / stampa KPI PDF», «KPI PDF»): ora sono paralleli,
`... Excel` e `... in PDF`, e i due che aprono la stampa del browser invece di
scaricare lo dicono nel suggerimento.

## Aperto su Analisi

- **I PDF passano dalla stampa del browser**, non da jsPDF come negli altri tre.
  È il punto vecchio, e c'è di più: le stampe di Analisi hanno intestazioni con
  gradiente viola→petrolio e bande colorate, mentre i PDF di Bilancio, LIPE e
  F24 sono in scala di grigi. Stesso mestiere, due estetiche.
- **Due tracciati per la stessa tabella KPI.** Il working paper la esporta con
  `Codice · Famiglia · Indicatore · Descrizione operativa · Valore · Unità ·
  Formula · Affidabilità · Note`; il file KPI a sé con `Codice · Famiglia · KPI ·
  Valore · Unità · Formula · Affidabilità · Nota · Segnalazione`. Nomi diversi
  per le stesse colonne, e una colonna per parte che l'altro non ha.
- **I nomi dei fogli Excel sono tagliati a metà parola** dal limite di 31
  caratteri: «5_Conto economico — EBITDA   EB», con tre spazi dove c'era una
  barra.
- **I badge della sidebar non dicono che cosa contano.** «2 Eccezioni ⑥» sono
  decisioni aperte, «4 KPI ⑥» sono indicatori incompleti, «Rendiconto ❗» è un
  avviso: tre significati, stessa forma, nessuna spiegazione.
- **Due meccanismi di download** nello stesso tool: il working paper passa da
  `XLSX.writeFile`, gli altri due da `XLSX.write` + blob.
- **`v18WorkbookLineage`**: da decidere se accendere il foglio di tracciabilità
  o toglierlo, visto che «Mappatura conti» copre già gran parte.

## Da portare agli altri tool

1. **Il vocabolario del backup** (`Scarica un backup` / `Ripristina da backup`):
   oggi Bilancio dice «Esporta archivio (JSON)», LIPE «Esporta backup completo»,
   il Fascicolo «Esporta backup JSON», F24 «Esporta backup».
2. **«Da dove vuoi partire?»**: Bilancio, Analisi e LIPE ce l'hanno con le tre
   strade; F24 ha la sua variante a tre schede; **Fascicolo e Confronto regimi
   non ce l'hanno affatto**, contro quello che dice il concept.
3. **La rifinitura dei numeri Excel** ora vale per i quattro tool che caricano
   `tal-xlsx-guard.js` (Bilancio, Analisi, LIPE, Confronto regimi): **F24 e
   Fascicolo non lo caricano** e vanno controllati a parte.
4. **Cercare le guardie morte anche negli altri tool**: `typeof X==='function'`
   con `X` chiuso in un IIFE è un difetto che non si vede leggendo, e in Analisi
   ce n'erano cinque.


---

# Giro 3 — gli altri cinque tool, 23 agosto 2026

Stesso metodo del giro 2: aprire i file prodotti e leggerne i byte, guardare il
testo reso invece del sorgente. Alla fine: **99 test node**, 42 controlli
responsive, contrasto pulito, zero apostrofi al posto degli accenti sul sito.

## Anche il bilancio da depositare usciva senza separatore

Il Bilancio civilistico stava peggio di Analisi: **316 numeri, zero formati** in
tutti e due gli export, compreso «Scarica bilancio (Excel)» — il prospetto di
deposito, il file che si consegna. Le immobilizzazioni si leggevano `385000`, e
il foglio non aveva nemmeno le larghezze delle colonne. LIPE: 111 numeri, zero
formati. F24, che la guardia non la caricava affatto: 22 numeri, zero formati.

La regola non è inventata, è letta da quello che i fogli già dichiarano:

| Dichiarazione nel foglio | Formato |
|---|---|
| intestazione che finisce con «€» | importo |
| intestazione con due date gg-mm-aaaa | importo nelle colonne sotto |
| nomi contabili: Importo, Imponibile, IVA, Saldo, Detrazione… | importo |
| intestazione che comincia per «%» | percentuale scritta come intero |
| fogli a due colonne | la dichiarazione sta nell'etichetta della riga |
| colonna «Unità» accanto a «Valore» (i KPI) | dall'unità dichiarata |

Un anno resta un anno: per non sbagliare serve che **sia** l'etichetta **sia**
l'intervallo 1900-2100 lo dicano, così «Compensi dell'anno = 120.000» non
diventa una data. Verificato sui file veri: Bilancio 316/316, LIPE 100/111 (gli
altri sono mesi e trimestri), Confronto regimi 212/214 (gli anni), F24 18/22 (i
conteggi di righe), Analisi invariata.

**La guardia ora la caricano tutti e sei.** F24 e Fascicolo non lo facevano — non
per scelta, era rimasto indietro — e restavano senza il controllo che il file
scaricato sia davvero un pacchetto XLSX valido.

## «3500,00 €» accanto a «40.620,55 €», nello stesso elenco

Trovato guardando lo schermo dell'F24. Non è un errore di chi ha scritto il
codice: `Intl.NumberFormat` ha `useGrouping: 'auto'`, e 'auto' segue il CLDR —
che **per l'italiano non raggruppa i numeri di quattro cifre**.

Il Bilancio e Confronto regimi lo dichiaravano già; Analisi, LIPE e F24 no.
Tre tool mostravano un numero e tre un altro, e negli Excel il formato
`#,##0.00` raggruppa comunque: schermo e file dicevano due cose diverse sullo
stesso importo. Dichiarato in tutti e nove i formattatori monetari, con un test
che se ne accorge — e che me ne ha subito trovato uno che avevo saltato.

## Il backup si chiama allo stesso modo in tutti e sei

Prima ognuno aveva il suo vocabolario, e due tool ne avevano più d'uno in casa:
il Bilancio tre, LIPE cinque. Ora si scarica un backup e si ripristina da
backup, con l'oggetto scritto quando ce n'è più d'uno. **La parola JSON non
compare più su nessun pulsante.**

## Le parole inglesi dentro l'interfaccia italiana

Letto il testo reso di ogni sezione, tool per tool.

- **Bilancio**: «mapping» → «mappatura» (nove punti, compresa la descrizione che
  finisce nei motori di ricerca, dove il catalogo diceva già «mappatura»);
  «export» → «esportazione», compresi quattro messaggi «Errore export Excel».
- **Analisi** chiamava il tool fratello col suo nome inglese: «L'import è
  identico a quello del **Financial Statement**». Ora dice Bilancio civilistico.
- **LIPE**: «Il cliente nasce dall'import» → «dal file»; «Prova gli output» →
  «Vedi i file prodotti»; il controllo «Associazione import-periodo» →
  «Coerenza fra file e periodo». E il titolo della pagina diceva «mapping IVA»
  mentre il tool in ogni schermata lo chiama codiciario.
- **Confronto regimi**: «Il pre-check del forfettario» → «Il controllo
  preliminare sul forfettario».

Restano dentro le parole che in italiano si usano davvero così: browser,
template, tool, budget, forecast, benchmark, cash flow, business unit, e
«Desktop Telematico», che è il nome del programma dell'Agenzia.

## Le guardie morte: solo Analisi

Cercate in tutti e sei. `financial-analysis` ne ha cinque; LIPE ne ha quattro e
il Bilancio una, ma **sono tutte attive**: lì il nome viene preso da `window`
una riga prima della guardia, che è il modo giusto di scriverle. Il difetto
resta confinato al file con le sette definizioni sovrapposte.

## Quattro volte ho misurato male

Vale la pena scriverlo, perché ogni volta stavo per dichiarare un difetto che
non c'era.

1. **Colonna sbagliata**: «l'export KPI non ha formati» — guardavo la colonna E,
   che in quel file è «Unità», non «Valore».
2. **Ho sostituito il livello che volevo misurare**: per catturare la cartella
   Excel avevo rimpiazzato `XLSX.writeFile`, che è esattamente il punto in cui
   la guardia applica la rifinitura. Misuravo il *prima*.
3. **Ho guardato solo la regione che mi aspettavo**: nel Fascicolo «Esporta»,
   «Dati e backup» e «Come si usa» sembravano morti perché `.app-main` non
   cambiava. Aprono finestre di dialogo, e funzionano.
4. **Ho avvolto `window.toast` mentre il codice chiama la `toast` lessicale**:
   il redirect di «4 Confronto» sembrava muto. Il messaggio c'è, ed è pure
   scritto bene: «Prima scegli la professione e il regime attuale: il confronto
   parte dal tuo punto di partenza.»

Regola: quando un risultato sembra peggiore del previsto, il primo sospetto è
la misura. E per verificare un output si apre l'output, non il codice che lo
produce né uno stadio intermedio.

## Ancora aperto

- **«Da dove vuoi partire?»** manca in **Fascicolo** e **Confronto regimi**: le
  tre strade ci sono tutte, ma presentate come pulsanti sparsi invece che nel
  blocco condiviso. È un cambio di forma, non di sostanza: non l'ho fatto senza
  poterlo guardare.
- **Nel Fascicolo, Esporta / Dati e backup / Come si usa sono finestre**, negli
  altri cinque sono sezioni con un indirizzo proprio. Stesso gruppo di sidebar,
  due modi di comportarsi.
- **I PDF**: Analisi passa dalla stampa del browser con intestazioni colorate,
  gli altri tre scaricano con jsPDF in scala di grigi.
- **Due tracciati per la stessa tabella KPI** in Analisi.
- **I nomi dei fogli Excel tagliati a metà parola** dal limite di 31 caratteri.
- **`v18WorkbookLineage`**: da decidere se accendere il foglio di tracciabilità
  o toglierlo, visto che «Mappatura conti» copre già gran parte.


---

# Giro 4 — le pagine editoriali, 23 agosto 2026

Le 58 pagine editoriali erano state viste solo con controlli automatici. Qui
sono state lette.

## Due articoli diversi si presentavano a Google con lo stesso titolo

È il difetto più serio del giro, e stava in piena vista: **su quattordici
pagine su quindici il titolo che il browser mostra era diverso da quello del
tag**.

La causa è la stessa dei dizionari morti del giro 1, ma con un effetto in più.
Quindici pagine portano un blocco di traduzione che all'avvio esegue
`setLang(...)`, e quella chiamata fa due cose che restano vere anche quando non
c'è più niente da tradurre:

```js
document.title = seo[lang].title;
meta[name=description].content = seo[lang].description;
```

I dizionari non sono stati aggiornati quando i titoli sono cambiati. Google
esegue il JavaScript: indicizzava quelli.

- «Analisi e riclassificazione gestionale del bilancio» si presentava come
  «Come riclassificare un bilancio IAS/IFRS secondo la IV Direttiva» — il
  titolo di un altro articolo. **Due pagine, un titolo solo.**
- In inglese si leggeva «under the Italian **Italian** statutory framework».
- Su sei pagine spariva il suffisso «| Tax Automation Lab» che hanno tutte le
  altre 45.

Il tag è la versione buona, e non è un'opinione: `og:title` e `twitter:title`
dicono la stessa cosa del tag su ogni pagina del sito. Quindi dove il blocco era
**morto** (6 pagine) l'ho tolto — 60 KB di dizionario, con verifica pagina per
pagina che testo reso e numero di nodi non cambino di un carattere — e dove era
**vivo** (9 pagine, 31-39 elementi tradotti davvero) ho allineato titolo e
descrizione del dizionario al tag.

`tests/titoli.test.mjs` lo tiene: su tutte le pagine `og:title` e
`twitter:title` devono dire quello che dice il tag, e dove un dizionario
riscrive il titolo deve scrivere esattamente il tag.

**Un errore evitato per un soffio.** La prima versione del controllo di
sicurezza cercava solo `data-i18n` e `data-lang`, e avrebbe tolto il blocco
**vivo** del Bilancio e di LIPE, che usano `data-tal-key` e `data-tal-fact`.

## Il resto

- **Il Fascicolo aveva due titoli**: «Fascicolo fiscale cliente» nella scheda del
  browser, «Fascicolo fiscale cliente: scheda fiscale permanente e per
  esercizio» quando se ne condivide il link. Ora dicono la stessa cosa, e la
  seconda è anche quella che segue il modo in cui si chiamano gli altri tool.
- **Gli apostrofi dei prompt**: dentro i `<pre>` di «Configura con AI»
  convivevano 58 apostrofi dritti e 41 tipografici — la stessa mescolanza degli
  accenti, nello stesso posto. Ora sono 99 tipografici, e i due prompt
  scaricabili sono cambiati insieme alla pagina.
- **«Mapping» esce anche dalle pagine editoriali**: tredici punti, con la
  concordanza da rifare (*il mapping utilizzato* → *la mappatura utilizzata*).
  In spagnolo diventa «asignación», che è il termine che il catalogo usa già.
- **Il badge «SOON»** del riquadro collaborazioni ora dice PRESTO in italiano e
  PRÓXIMAMENTE in spagnolo.
- **Sitemap rigenerato**: 45 URL, esattamente le 45 pagine indicizzabili,
  nessun alias `noindex` dentro, nessuna voce senza file.

## Quello che ho verificato e NON era un difetto

- **Le descrizioni social diverse dalla meta description** su tre tool: non è
  deriva, è scelta. La `description` è scritta per i motori di ricerca («Tool
  gratuito per confrontare…»), la `og:description` per chi vede il link
  condiviso («Quanto ti rimane davvero fra forfettario, ordinario e società»).
  Il titolo invece deve essere uno solo, ed è quello che ora il test controlla.
- **Gli anglicismi delle pagine di metodo** — tool, file, template, prompt,
  input, output, test, report, audit, brief, changelog, PASS/FAIL — restano.
  Toglierli non renderebbe il testo più mio: lo renderebbe finto. Stessa cosa
  per «compliance», «reporting» e «M&A» nella biografia, che sono i nomi delle
  cose che faccio.
- **Le pagine inglese e spagnola di «Configura con AI»**: sembravano prive del
  Fascicolo. Ce l'hanno — le ultime tre schede nascono da un
  `insertAdjacentHTML` in coda al file. Me ne sono accorto dopo averla
  aggiunta, quando in pagina ne sono comparse due con lo stesso `id`. Il test
  aveva intanto bocciato l'aggiunta perché il testo inglese conteneva `year's`
  e quell'apostrofo chiudeva la stringa JavaScript: la trappola 6 di
  `CONTESTO.md`, viva.


---

# Giro 5 — i PDF, aperti davvero, 23 agosto 2026

Era il punto aperto più vecchio: *«i PDF: aprirli davvero, uno per tool»*. In
questo ambiente non si possono guardare le pagine come immagini, ma si può fare
di meglio che fidarsi: **catturare il file che il browser sta per scaricare,
salvarlo su disco e rileggerne il testo dai flussi di contenuto**. È quello che
il professionista si ritrova in mano.

## Bilancio civilistico — prospetto di deposito

Catturato il PDF di «Scarica bilancio (PDF)»: 51.443 byte, due pagine. Riletto
tutto. È corretto, e vale la pena dire in che senso:

- schema abbreviato, art. 2435-bis c.c., con i dati anagrafici in testa;
- Stato patrimoniale attivo e passivo che quadrano a 687.200 su entrambi gli
  esercizi, Conto economico fino all'utile di 118.100;
- importi con il separatore delle migliaia e i negativi fra parentesi —
  `(12.000)` per gli oneri finanziari — che è la convenzione dei prospetti;
- numerazione delle voci IV Direttiva corretta, comprese le sottovoci
  (`5) altri ricavi e proventi` → `b) altri`);
- piede con «Alfa Industriale S.r.l. · Bilancio al 31-12-2025 · Pag. 1 di 2».

**Un falso allarme, il sesto della giornata.** Nel testo estratto sembrava
esserci un doppio spazio dove il sorgente ha un trattino lungo — «Schema
abbreviato␣␣art. 2435-bis». Sembrava che jsPDF perdesse i caratteri fuori dal
Latin-1. Guardando i byte veri: c'è `0x97`, che in `WinAnsiEncoding` **è** il
trattino lungo. Il terminale non lo mostrava. Nel PDF non manca niente: zero
stringhe a due byte, cioè nessun carattere è finito nella codifica di ripiego.

## Generatore F24 — modello ministeriale

Catturato il PDF di un F24: 604.802 byte, tre pagine (le copie del modello).
Il modello ministeriale è un'immagine da 2481×3508 — A4 a 300 dpi — **incorporata
una volta sola** e riusata, non una copia per pagina.

I valori sopra non sono disegnati: sono **testo vero**, quindi il PDF resta
cercabile e copiabile. Riletti dai flussi compressi: `ALFA S.R.L.`, `Milano`,
`MI`, `Via Roma 12`, codice tributo `1040` periodo `0008` anno `2026` importo
`5500`+`00`, il credito `6099` anno `2025` importo `2000`+`00`, i totali e il
saldo `+` `3500`+`00`. I centesimi stanno nelle caselle separate, come vuole il
modello.

## Come si rifà

Lo strumento è in `tests/` solo di fatto, non ancora come test: si intercetta
`URL.createObjectURL`, si prende il Blob, lo si porta a base64 e lo si decodifica
su file; poi un estrattore di una cinquantina di righe legge i flussi (Flate) e
tira fuori le stringhe fra parentesi. Per i flussi compressi conviene tagliare
con `/Length` del dizionario invece di cercare `endstream`, che dentro
un'immagine capita per caso.

---

## Ancora da guardare

- i PDF di LIPE e del Fascicolo, con lo stesso metodo del giro 5;
- il mobile su dispositivo vero: l'armatura misura Chromium, che non ha
  `env(safe-area-inset-*)`;
- il modulo di controllo dell'Agenzia per il tracciato F24A0 e per il file LIPE;
- «Da dove vuoi partire?» in Fascicolo e Confronto regimi: le tre strade ci
  sono, ma non nel blocco condiviso. È un cambio di forma, da guardare.
