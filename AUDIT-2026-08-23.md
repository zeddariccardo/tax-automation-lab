# Audit del sito — 23 e 24 agosto 2026

Sei giri: il **guscio del sito**, poi **Analisi di bilancio** a fondo, poi **gli
altri cinque tool**, poi le **pagine editoriali**, poi i **PDF**, poi i **tasti
sul telefono**. I sei verbali sono in fila qui sotto.

## Giro 1 — il guscio

Perimetro del giro: il **guscio del sito** (66 pagine HTML), il **catalogo dei
tool**, i **documenti legali**, le **librerie incorporate** e una lettura del
**testo che l'utente legge**. I sei tool sono stati aperti e navigati; l'esame
funzionale voce per voce è rimandato al giro 2.

Base di partenza, verificata prima di toccare qualsiasi cosa: 83 test node
superati, 42 controlli responsive superati, contrasto senza fallimenti.
Alla fine del giro: **90 test node, 42 responsive, contrasto pulito.**

---

## 1. La libreria incorporata era stata sovrascritta dal codice del tool

È il difetto più serio trovato, e non era nell'elenco di nessuno.

In `tools/financial-analysis/index.html`, **dentro** il bundle minificato di
SheetJS, al posto della funzione `tr` della libreria c'era questo:

```js
var er;function rr(e){er=e}function talReclassRow(label,c,p,cls=''){
  const hasPrev=!!(STATE.periods&&STATE.periods.previous&&STATE.periods.previous.available);
  ...
}function ar(e,r,t){...}
```

`talReclassRow` è codice del tool — legge `STATE.periods`, chiama `esc`, `money`,
`pct`. Una sostituzione larga su un file da 1,7 MB era andata a segno nel punto
sbagliato, mangiandosi `function tr(e)`, che nel file non esisteva più.

**Conseguenza vera, riproducibile.** La versione di `talReclassRow` finita nel
bundle era quella **buona**: gestisce il caso in cui non c'è l'esercizio
precedente. La versione che girava era la vecchia, senza quella guardia. Misurato
in pagina, senza esercizio precedente:

| Voce | Corrente | Precedente | Δ | Δ% |
|---|---|---|---|---|
| Attivo immobilizzato | 150.000 € | — | **150.000 €** | — |

La colonna della variazione mostrava l'intero saldo corrente come se fosse una
crescita, con la colonna del precedente vuota. Chi legge vede
«immobilizzazioni +150.000» dove non c'è alcun termine di paragone.

Che `tr` fosse sparita non rompeva nulla — non viene mai chiamata dal ramo
browser — ma era una mina: al primo aggiornamento della libreria o al primo
percorso di codice che la usa, il tool si sarebbe rotto in silenzio.

**Corretto:** la definizione viva di `talReclassRow` è ora quella con la
guardia. Verificato in pagina: senza esercizio precedente le tre colonne di
confronto dicono «—»; con l'esercizio precedente dicono
`150.000 € · 120.000 € · 30.000 € · 25,0%`.

## 2. La firma di SheetJS valeva per un tool su sei

`THIRD-PARTY-NOTICES.txt` pubblicava una sola firma SHA-256 per la copia di
SheetJS «embedded in each operational tool HTML». Misurata: corrispondeva **solo
al Generatore F24**. Normalizzando i fine riga, i sei tool incorporavano
**quattro sequenze di byte diverse**:

| Tool | Copia |
|---|---|
| f24 | quella dichiarata |
| lipe, tfa-client-file, confronto-regimi | identiche fra loro, 6 byte in meno |
| financial-statement | come sopra, più la sotto-libreria `codepage` a 1.3.4 invece di 1.2.0 |
| financial-analysis | quella col codice del tool dentro |

I 6 byte mancanti sono **due caratteri a larghezza zero** (U+200C e U+200D) in
una tabella di codepage: la firma di un testo passato attraverso qualcosa che
normalizza i caratteri invisibili, non di una versione diversa della libreria.

**Corretto:** i sei tool incorporano ora la stessa identica copia, quella che
corrisponde alla firma pubblicata. Verificato in pagina su tutti e sei che
SheetJS 0.20.3 legge e scrive: `XLSX.write` → `XLSX.read` con accenti italiani
torna identico, e i sei producono lo stesso file di 15.953 byte.

## 3. Un test nuovo, che questa classe di difetti non la fa più passare

`tests/vendored-libraries.test.mjs`, quattro controlli:

1. i sei tool incorporano la stessa identica copia di SheetJS;
2. quella copia è la firma dichiarata nei notices;
3. **nel bundle non c'è codice del tool** (funzioni `tal*`, letture di `STATE`);
4. il tag `<script>` dichiara `data-vendored="SheetJS 0.20.3"`.

Provato che falliscono davvero: rimettendo il file di prima, tutti e quattro
diventano rossi con il messaggio giusto.

## 4. Le versioni nei tre cataloghi erano ferme, e nessun test le guardava

`tools/manifest.json` è l'elenco autorevole e il test esistente lo confronta con
le pagine dei tool. Ma il **badge che l'utente legge** sta nelle pagine di
catalogo, fuori da ogni controllo:

| Tool | Manifest | `/tools/` | `/en/tools/` e `/es/tools/` |
|---|---|---|---|
| Bilancio civilistico | 1.4.1 | 1.4.0 | **1.3.6** |
| Analisi di bilancio | 2.1.0 | 2.1.0 | **2.0.6** |
| Generatore LIPE | 3.6.1 | 3.5.0 | **3.4.3** |
| Fascicolo, F24, Confronto regimi | — | corretti | corretti |

Corretti tutti e tre. E `tests/version-coherence.test.mjs` ora legge le sei
schede di ciascun catalogo, ne estrae badge e `slug`, e li confronta col
manifest — anche questo verificato che fallisce quando deve.

## 5. «Configura con AI»: gli accenti scritti con l'apostrofo

La pagina mescolava le due grafie **nello stesso paragrafo**: «cioè» e «ciò»
accanto a «piu'», «e'», «c'e'». Non era una scelta: i tre prompt più vecchi
(Bilancio, Analisi, LIPE) erano quasi corretti, i tre più recenti (Fascicolo,
F24, Confronto regimi) quasi interamente in apostrofo. La pagina spagnola
equivalente non ha un solo apostrofo al posto di un accento.

- `configura-con-ai/index.html`: **160 correzioni**
- `resources/templates/Prompt_F24_AI_v1.0.txt`: 39
- `resources/templates/Prompt_Fascicolo_Fiscale_Cliente_AI_v1.0.txt`: 36

Comprese le maiuscole (`SOCIETA'` → `SOCIETÀ`) e le elisioni composte (`c'e'` →
`c'è`, `com'e'` → `com'è`, `dell'entita'` → `dell'entità`). Restano intatti gli
apostrofi veri (`all'`, `un po'`, `dell'`).

Il test `prompt F24: il .txt scaricabile coincide con quello della pagina` ha
fatto il suo mestiere: ha bocciato la correzione fatta a metà, quando avevo
sistemato la pagina e non ancora il file scaricabile.

## 6. Otto pulsanti «chiudi» senza nome accessibile

Nello stesso tool convivevano `<button class="close-btn" aria-label="Chiudi">` e
`<button class="close-btn">`, entrambi con la sola crocetta `×`: il secondo, con
lo screen reader, si annuncia «times». Aggiunto `aria-label="Chiudi"` ai sette
del Bilancio e all'uno di Analisi.

## 7. Documenti legali e testo residuo

- `THIRD-PARTY-NOTICES.txt` diceva «The **five** operational tools» e la tabella
  per tool non elencava Confronto regimi. Aggiunta la riga, aggiornata la data,
  e aggiunta la nota su cosa è successo alle copie di SheetJS.
- `tools/convenienza-fiscale/index.html`: «Il tool **e'** lo stesso» → «è».

---

## Quello che ho verificato e NON era un difetto

Vale la pena scriverlo: sono le trappole in cui si ricasca.

- **Le versioni sulle tre home** (`v1.3.4`, `v3.4.1`) sembrano vecchissime a
  `grep`, ma **non sono rese**: stanno in un dizionario di traduzione che non ha
  più elementi `[data-i18n]` a cui applicarsi. Misurato in pagina: zero
  `[data-i18n]`, zero `[data-lang]`.
- **`tools/tfa-client-file`** sembra avere una sidebar mutilata: la prima
  `.workspace-nav` ha tre voci. Ne ha **cinque**, di `.workspace-nav`, ed è
  completa.
- **LIPE è allineato al concept**, contro quello che diceva questo repository:
  ha «Dati e backup», «Come si usa» e i deep link (`#/clienti` →
  `#/quadro-vp`, con Indietro e Avanti che camminano fra le sezioni). Punto
  chiuso in `CONTESTO.md`.
- **I sei tool non fanno una sola richiesta fuori origine.**
- **Nessuno scorrimento orizzontale** e comando del menu 44×44 corretto su
  6 tool × 7 larghezze. Nessun testo sotto il rapporto di contrasto 3,0.

---

## Deciso in questo giro

- **Il percorso numerato non è più un'eccezione dell'F24: è una regola.** I tool
  che preparano qualcosa con un ordine vincolante usano `PERCORSO` a passi
  numerati; gli altri la sidebar `Configurazione / Lavoro / Risultati e dati`.
  Confronto regimi resta com'è. Scritto in `CONTESTO.md`.

## Aperto — messo in coda per il giro 2

Tutti e quattro decisi da Riccardo il 23 agosto, da fare nel prossimo giro:

1. **Uniformare i `?v=` degli asset.** Oggi la regola vale davvero solo per
   `tal-design.css`:

   | Asset | Stato |
   |---|---|
   | `tal-design.css` | 61 riferimenti, tutti `?v=20260814` ✔ |
   | `tal-experience.js` | 56 a `?v=20260812`, **4 a `?v=20260813`** |
   | `site-shell.css` | **51 senza `?v=`**, 4 a `20260810-v61510-header-1`, 2 a `20260813`, 1 a `20260811-ctp-230` |
   | `site-shell.js` | 4 riferimenti, nessun `?v=` |

2. **Togliere il codice morto.** Otto pagine portano un dizionario di traduzione
   a tre lingue con la sua `setLang`, e **zero elementi a cui applicarlo**. Nella
   home ci sono anche regole CSS per blocchi che non esistono più
   (`.author-signature`, `.home-privacy-badge`, `.home-quick-links`). È il motivo
   per cui un `grep` sulle versioni dà risposte sbagliate.

3. **`canonical` sulle 12 pagine di avviso.** `/en/tools/<slug>/` e
   `/es/tools/<slug>/` sono le uniche 12 pagine del sito senza `canonical`: va
   fatto puntare alla versione italiana.

4. **Le date «Aggiornato il» del catalogo.** Quattro tool su sei dicono ancora
   «10 agosto 2026». Il Bilancio è stato modificato il 14, LIPE il 14 e dopo,
   Analisi e Fascicolo il 21-22, Confronto regimi il 23. Da allineare e da
   mettere sotto test.

## Aperto — trovato e non ancora deciso

- **Il Bilancio numera le stesse sezioni in due modi.**

  | Sidebar | Intestazione della pagina |
  |---|---|
  | ✎ Anagrafica | **I ·** ANAGRAFICA DELLA SOCIETÀ |
  | ⇩ Importa da Excel | **II ·** IMPORT DEL BILANCIO |
  | **1** Bilancio ITA GAAP | **III ·** BILANCIO ITA GAAP |

  Chi legge «1 Bilancio» nella sidebar arriva su una pagina intitolata «III». E
  l'intestazione dice «IMPORT del bilancio» dove la sidebar dice «Importa».

- **Le icone della stessa azione cambiano da tool a tool.** «Esporta» è `↧` nel
  Bilancio, in LIPE e nel Fascicolo, ma `P` in Analisi. I passi di lavoro sono
  cifre arabe nel Bilancio e in Analisi, numeri romani in LIPE (`I`, `II`).

- **Confronto regimi è l'unico tool che non produce un PDF**: solo Excel. Resta
  aperto anche il punto vecchio su Analisi, che stampa dal browser invece di
  scaricare con jsPDF.

- **La home chiama jsDelivr, la pagina privacy non lo dice.** L'intro importa
  `threejs-components@0.0.19` da `cdn.jsdelivr.net` come modulo dinamico. È
  documentato nei *Third-party notices* (sezione 3) ed è decorativo, con ripiego
  se fallisce. Ma la pagina Privacy elenca i fornitori «tra cui GitHub Pages e
  Cloudflare» e non nomina jsDelivr, che riceve l'IP di ogni visitatore alla
  prima apertura della home. O si nomina, o si ospita il modulo in `assets/`.

---

# Giro 2 — Analisi di bilancio, 23 agosto 2026

Aperte tutte e ventuno le sezioni, premuto ogni pulsante, e **aperti davvero i
file prodotti**: non il codice che li genera, ma i byte che arrivano al browser,
riletti con `XLSX.read`. È la differenza che ha fatto trovare quasi tutto.

Alla fine: 93 test node, 42 controlli responsive, contrasto pulito, zero errori
in console attraversando tutte le sezioni.

## Le prime tre decisioni, chiuse

- **Numerazione.** Tolti i numeri romani dalle intestazioni di Bilancio, LIPE e
  Analisi. Erano tre sistemi incompatibili con le rispettive sidebar: chi
  leggeva «1 Bilancio» arrivava su una pagina intitolata «III», LIPE numerava
  I, III, IV, V (il II non esisteva), Analisi aveva **due** sezioni «III».
  Ora l'ordine lo dice la sidebar e l'intestazione dice il nome, uguale.
- **Icona di «Esporta»** in Analisi: era `P`, ora è `↧` come negli altri tre.
- **jsDelivr** nominato nella pagina Privacy, in tutte e tre le lingue.

## Quello che non si vedeva dal codice

### Il formato dei numeri negli Excel

Tre fogli su dieci del working paper e **tutti e sei** quelli delle analisi
avanzate uscivano senza un formato numerico: un current ratio si leggeva
`1,8873239436619718`, la mappatura conti mostrava gli importi senza separatore.
I sei prospetti riclassificati erano invece formattati bene — lo standard c'era,
mancava applicarlo al resto.

La rifinitura sta in `assets/tal-xlsx-guard.js`, che era già il punto unico da
cui passa la scrittura. La versione precedente indirizzava le celle **per
posizione fissa** (`B7:D12`) e cercava il foglio per nome: bastava una riga in
più perché formattasse le celle sbagliate — ed è quello che è successo appena ho
rinominato il foglio. Ora cerca la riga di intestazione e prende il formato
dall'**unità di misura che i KPI già dichiarano**, che è l'unico modo esatto di
sapere se `0,48` è «48,4%» o «0,48x». Gli anni restano numeri nudi.

### Cinque guardie che non scattano mai

In `financial-analysis` cinque blocchi di correzione sono protetti da
`typeof X === 'function'` dove `X` sta **dentro un altro IIFE**: `typeof`
risponde `'undefined'` e il blocco non viene mai eseguito.

| Nome | Cosa doveva fare | Stato |
|---|---|---|
| `v20DashboardRows` | rinominare «Business Health Score», riscrivere la nota, aggiungere la copertura benchmark | **corretto alla fonte** |
| `v18Write` | messaggio di conferma dopo l'export e guardia contro il doppio clic | **messaggio aggiunto alla fonte** |
| `v18WorkbookLineage` | aggiungere un foglio di tracciabilità dei conti | da decidere: duplicherebbe «Mappatura conti» |
| `v20AddIncidence` | colonne di incidenza a schermo | **non serve**: un'altra strada le mette già |
| `v18RequireExportGate` | gate di export | **non serve**: il gate vivo è `gate()` in `protect()` |

Le prime due erano difetti veri e visibili: la dashboard nell'Excel diceva
ancora «Business Health Score sperimentale» in inglese, e «Working paper Excel»
era **l'unico export del tool che non diceva niente** dopo aver scaricato il
file.

### Cinque nomi per lo stesso backup

Contati sui pulsanti resi: `exportConfig()` si chiamava in **tre** modi diversi a
seconda della schermata, `exportAllConfigs()` in **due**.

| Dove | Prima | Ora |
|---|---|---|
| Anagrafica | Esporta configurazione JSON | Scarica un backup di questa società |
| Dati e backup | Esporta società (JSON) | Scarica un backup di questa società |
| Esporta | Backup JSON società | Scarica un backup di questa società |
| Clienti | Esporta archivio JSON | Scarica un backup dell'archivio |
| Dati e backup | Esporta archivio (JSON) | Scarica un backup dell'archivio |

Adottato il vocabolario di **Confronto regimi**, che è il più recente e il più
leggibile: *si scarica un backup, si ripristina da backup*. Vale come canone per
la suite: gli altri quattro tool hanno ancora il loro (vedi sotto).

### Il gergo

«i gate di qualità», «Gate professionale», «Import transazionale», «Cash
conversion proxy», «Pacchetto di reporting», «Executive dashboard», «report con
lineage», e **«proxy» sedici volte** nel rendiconto finanziario e nella crisi
d'impresa. «Proxy» è diventato «stima»: è quello che è, ed è la parola che un
commercialista userebbe parlando.

Restano dentro i termini che in italiano si usano davvero così: budget,
forecast, benchmark, cash flow, template, business unit.

E i sei pulsanti di export si chiamavano in quattro modi diversi («Esporta KPI
Excel», «KPI Excel», «Esporta / stampa KPI PDF», «KPI PDF»): ora sono paralleli,
`... Excel` e `... in PDF`, e i due che aprono la stampa del browser invece di
scaricare lo dicono nel suggerimento.

## Aperto su Analisi

- **I PDF passano dalla stampa del browser**, non da jsPDF come negli altri tre.
  È il punto vecchio, e c'è di più: le stampe di Analisi hanno intestazioni con
  gradiente viola→petrolio e bande colorate, mentre i PDF di Bilancio, LIPE e
  F24 sono in scala di grigi. Stesso mestiere, due estetiche.
- **Due tracciati per la stessa tabella KPI.** Il working paper la esporta con
  `Codice · Famiglia · Indicatore · Descrizione operativa · Valore · Unità ·
  Formula · Affidabilità · Note`; il file KPI a sé con `Codice · Famiglia · KPI ·
  Valore · Unità · Formula · Affidabilità · Nota · Segnalazione`. Nomi diversi
  per le stesse colonne, e una colonna per parte che l'altro non ha.
- **I nomi dei fogli Excel sono tagliati a metà parola** dal limite di 31
  caratteri: «5_Conto economico — EBITDA   EB», con tre spazi dove c'era una
  barra.
- **I badge della sidebar non dicono che cosa contano.** «2 Eccezioni ⑥» sono
  decisioni aperte, «4 KPI ⑥» sono indicatori incompleti, «Rendiconto ❗» è un
  avviso: tre significati, stessa forma, nessuna spiegazione.
- **Due meccanismi di download** nello stesso tool: il working paper passa da
  `XLSX.writeFile`, gli altri due da `XLSX.write` + blob.
- **`v18WorkbookLineage`**: da decidere se accendere il foglio di tracciabilità
  o toglierlo, visto che «Mappatura conti» copre già gran parte.

## Da portare agli altri tool

1. **Il vocabolario del backup** (`Scarica un backup` / `Ripristina da backup`):
   oggi Bilancio dice «Esporta archivio (JSON)», LIPE «Esporta backup completo»,
   il Fascicolo «Esporta backup JSON», F24 «Esporta backup».
2. **«Da dove vuoi partire?»**: Bilancio, Analisi e LIPE ce l'hanno con le tre
   strade; F24 ha la sua variante a tre schede; **Fascicolo e Confronto regimi
   non ce l'hanno affatto**, contro quello che dice il concept.
3. **La rifinitura dei numeri Excel** ora vale per i quattro tool che caricano
   `tal-xlsx-guard.js` (Bilancio, Analisi, LIPE, Confronto regimi): **F24 e
   Fascicolo non lo caricano** e vanno controllati a parte.
4. **Cercare le guardie morte anche negli altri tool**: `typeof X==='function'`
   con `X` chiuso in un IIFE è un difetto che non si vede leggendo, e in Analisi
   ce n'erano cinque.


---

# Giro 3 — gli altri cinque tool, 23 agosto 2026

Stesso metodo del giro 2: aprire i file prodotti e leggerne i byte, guardare il
testo reso invece del sorgente. Alla fine: **99 test node**, 42 controlli
responsive, contrasto pulito, zero apostrofi al posto degli accenti sul sito.

## Anche il bilancio da depositare usciva senza separatore

Il Bilancio civilistico stava peggio di Analisi: **316 numeri, zero formati** in
tutti e due gli export, compreso «Scarica bilancio (Excel)» — il prospetto di
deposito, il file che si consegna. Le immobilizzazioni si leggevano `385000`, e
il foglio non aveva nemmeno le larghezze delle colonne. LIPE: 111 numeri, zero
formati. F24, che la guardia non la caricava affatto: 22 numeri, zero formati.

La regola non è inventata, è letta da quello che i fogli già dichiarano:

| Dichiarazione nel foglio | Formato |
|---|---|
| intestazione che finisce con «€» | importo |
| intestazione con due date gg-mm-aaaa | importo nelle colonne sotto |
| nomi contabili: Importo, Imponibile, IVA, Saldo, Detrazione… | importo |
| intestazione che comincia per «%» | percentuale scritta come intero |
| fogli a due colonne | la dichiarazione sta nell'etichetta della riga |
| colonna «Unità» accanto a «Valore» (i KPI) | dall'unità dichiarata |

Un anno resta un anno: per non sbagliare serve che **sia** l'etichetta **sia**
l'intervallo 1900-2100 lo dicano, così «Compensi dell'anno = 120.000» non
diventa una data. Verificato sui file veri: Bilancio 316/316, LIPE 100/111 (gli
altri sono mesi e trimestri), Confronto regimi 212/214 (gli anni), F24 18/22 (i
conteggi di righe), Analisi invariata.

**La guardia ora la caricano tutti e sei.** F24 e Fascicolo non lo facevano — non
per scelta, era rimasto indietro — e restavano senza il controllo che il file
scaricato sia davvero un pacchetto XLSX valido.

## «3500,00 €» accanto a «40.620,55 €», nello stesso elenco

Trovato guardando lo schermo dell'F24. Non è un errore di chi ha scritto il
codice: `Intl.NumberFormat` ha `useGrouping: 'auto'`, e 'auto' segue il CLDR —
che **per l'italiano non raggruppa i numeri di quattro cifre**.

Il Bilancio e Confronto regimi lo dichiaravano già; Analisi, LIPE e F24 no.
Tre tool mostravano un numero e tre un altro, e negli Excel il formato
`#,##0.00` raggruppa comunque: schermo e file dicevano due cose diverse sullo
stesso importo. Dichiarato in tutti e nove i formattatori monetari, con un test
che se ne accorge — e che me ne ha subito trovato uno che avevo saltato.

## Il backup si chiama allo stesso modo in tutti e sei

Prima ognuno aveva il suo vocabolario, e due tool ne avevano più d'uno in casa:
il Bilancio tre, LIPE cinque. Ora si scarica un backup e si ripristina da
backup, con l'oggetto scritto quando ce n'è più d'uno. **La parola JSON non
compare più su nessun pulsante.**

## Le parole inglesi dentro l'interfaccia italiana

Letto il testo reso di ogni sezione, tool per tool.

- **Bilancio**: «mapping» → «mappatura» (nove punti, compresa la descrizione che
  finisce nei motori di ricerca, dove il catalogo diceva già «mappatura»);
  «export» → «esportazione», compresi quattro messaggi «Errore export Excel».
- **Analisi** chiamava il tool fratello col suo nome inglese: «L'import è
  identico a quello del **Financial Statement**». Ora dice Bilancio civilistico.
- **LIPE**: «Il cliente nasce dall'import» → «dal file»; «Prova gli output» →
  «Vedi i file prodotti»; il controllo «Associazione import-periodo» →
  «Coerenza fra file e periodo». E il titolo della pagina diceva «mapping IVA»
  mentre il tool in ogni schermata lo chiama codiciario.
- **Confronto regimi**: «Il pre-check del forfettario» → «Il controllo
  preliminare sul forfettario».

Restano dentro le parole che in italiano si usano davvero così: browser,
template, tool, budget, forecast, benchmark, cash flow, business unit, e
«Desktop Telematico», che è il nome del programma dell'Agenzia.

## Le guardie morte: solo Analisi

Cercate in tutti e sei. `financial-analysis` ne ha cinque; LIPE ne ha quattro e
il Bilancio una, ma **sono tutte attive**: lì il nome viene preso da `window`
una riga prima della guardia, che è il modo giusto di scriverle. Il difetto
resta confinato al file con le sette definizioni sovrapposte.

## Quattro volte ho misurato male

Vale la pena scriverlo, perché ogni volta stavo per dichiarare un difetto che
non c'era.

1. **Colonna sbagliata**: «l'export KPI non ha formati» — guardavo la colonna E,
   che in quel file è «Unità», non «Valore».
2. **Ho sostituito il livello che volevo misurare**: per catturare la cartella
   Excel avevo rimpiazzato `XLSX.writeFile`, che è esattamente il punto in cui
   la guardia applica la rifinitura. Misuravo il *prima*.
3. **Ho guardato solo la regione che mi aspettavo**: nel Fascicolo «Esporta»,
   «Dati e backup» e «Come si usa» sembravano morti perché `.app-main` non
   cambiava. Aprono finestre di dialogo, e funzionano.
4. **Ho avvolto `window.toast` mentre il codice chiama la `toast` lessicale**:
   il redirect di «4 Confronto» sembrava muto. Il messaggio c'è, ed è pure
   scritto bene: «Prima scegli la professione e il regime attuale: il confronto
   parte dal tuo punto di partenza.»

Regola: quando un risultato sembra peggiore del previsto, il primo sospetto è
la misura. E per verificare un output si apre l'output, non il codice che lo
produce né uno stadio intermedio.

## Ancora aperto

- **«Da dove vuoi partire?»** manca in **Fascicolo** e **Confronto regimi**: le
  tre strade ci sono tutte, ma presentate come pulsanti sparsi invece che nel
  blocco condiviso. È un cambio di forma, non di sostanza: non l'ho fatto senza
  poterlo guardare.
- **Nel Fascicolo, Esporta / Dati e backup / Come si usa sono finestre**, negli
  altri cinque sono sezioni con un indirizzo proprio. Stesso gruppo di sidebar,
  due modi di comportarsi.
- **I PDF**: Analisi passa dalla stampa del browser con intestazioni colorate,
  gli altri tre scaricano con jsPDF in scala di grigi.
- **Due tracciati per la stessa tabella KPI** in Analisi.
- **I nomi dei fogli Excel tagliati a metà parola** dal limite di 31 caratteri.
- **`v18WorkbookLineage`**: da decidere se accendere il foglio di tracciabilità
  o toglierlo, visto che «Mappatura conti» copre già gran parte.


---

# Giro 4 — le pagine editoriali, 23 agosto 2026

Le 58 pagine editoriali erano state viste solo con controlli automatici. Qui
sono state lette.

## Due articoli diversi si presentavano a Google con lo stesso titolo

È il difetto più serio del giro, e stava in piena vista: **su quattordici
pagine su quindici il titolo che il browser mostra era diverso da quello del
tag**.

La causa è la stessa dei dizionari morti del giro 1, ma con un effetto in più.
Quindici pagine portano un blocco di traduzione che all'avvio esegue
`setLang(...)`, e quella chiamata fa due cose che restano vere anche quando non
c'è più niente da tradurre:

```js
document.title = seo[lang].title;
meta[name=description].content = seo[lang].description;
```

I dizionari non sono stati aggiornati quando i titoli sono cambiati. Google
esegue il JavaScript: indicizzava quelli.

- «Analisi e riclassificazione gestionale del bilancio» si presentava come
  «Come riclassificare un bilancio IAS/IFRS secondo la IV Direttiva» — il
  titolo di un altro articolo. **Due pagine, un titolo solo.**
- In inglese si leggeva «under the Italian **Italian** statutory framework».
- Su sei pagine spariva il suffisso «| Tax Automation Lab» che hanno tutte le
  altre 45.

Il tag è la versione buona, e non è un'opinione: `og:title` e `twitter:title`
dicono la stessa cosa del tag su ogni pagina del sito. Quindi dove il blocco era
**morto** (6 pagine) l'ho tolto — 60 KB di dizionario, con verifica pagina per
pagina che testo reso e numero di nodi non cambino di un carattere — e dove era
**vivo** (9 pagine, 31-39 elementi tradotti davvero) ho allineato titolo e
descrizione del dizionario al tag.

`tests/titoli.test.mjs` lo tiene: su tutte le pagine `og:title` e
`twitter:title` devono dire quello che dice il tag, e dove un dizionario
riscrive il titolo deve scrivere esattamente il tag.

**Un errore evitato per un soffio.** La prima versione del controllo di
sicurezza cercava solo `data-i18n` e `data-lang`, e avrebbe tolto il blocco
**vivo** del Bilancio e di LIPE, che usano `data-tal-key` e `data-tal-fact`.

## Il resto

- **Il Fascicolo aveva due titoli**: «Fascicolo fiscale cliente» nella scheda del
  browser, «Fascicolo fiscale cliente: scheda fiscale permanente e per
  esercizio» quando se ne condivide il link. Ora dicono la stessa cosa, e la
  seconda è anche quella che segue il modo in cui si chiamano gli altri tool.
- **Gli apostrofi dei prompt**: dentro i `<pre>` di «Configura con AI»
  convivevano 58 apostrofi dritti e 41 tipografici — la stessa mescolanza degli
  accenti, nello stesso posto. Ora sono 99 tipografici, e i due prompt
  scaricabili sono cambiati insieme alla pagina.
- **«Mapping» esce anche dalle pagine editoriali**: tredici punti, con la
  concordanza da rifare (*il mapping utilizzato* → *la mappatura utilizzata*).
  In spagnolo diventa «asignación», che è il termine che il catalogo usa già.
- **Il badge «SOON»** del riquadro collaborazioni ora dice PRESTO in italiano e
  PRÓXIMAMENTE in spagnolo.
- **Sitemap rigenerato**: 45 URL, esattamente le 45 pagine indicizzabili,
  nessun alias `noindex` dentro, nessuna voce senza file.

## Quello che ho verificato e NON era un difetto

- **Le descrizioni social diverse dalla meta description** su tre tool: non è
  deriva, è scelta. La `description` è scritta per i motori di ricerca («Tool
  gratuito per confrontare…»), la `og:description` per chi vede il link
  condiviso («Quanto ti rimane davvero fra forfettario, ordinario e società»).
  Il titolo invece deve essere uno solo, ed è quello che ora il test controlla.
- **Gli anglicismi delle pagine di metodo** — tool, file, template, prompt,
  input, output, test, report, audit, brief, changelog, PASS/FAIL — restano.
  Toglierli non renderebbe il testo più mio: lo renderebbe finto. Stessa cosa
  per «compliance», «reporting» e «M&A» nella biografia, che sono i nomi delle
  cose che faccio.
- **Le pagine inglese e spagnola di «Configura con AI»**: sembravano prive del
  Fascicolo. Ce l'hanno — le ultime tre schede nascono da un
  `insertAdjacentHTML` in coda al file. Me ne sono accorto dopo averla
  aggiunta, quando in pagina ne sono comparse due con lo stesso `id`. Il test
  aveva intanto bocciato l'aggiunta perché il testo inglese conteneva `year's`
  e quell'apostrofo chiudeva la stringa JavaScript: la trappola 6 di
  `CONTESTO.md`, viva.


---

# Giro 5 — i PDF, aperti davvero, 23 agosto 2026

Era il punto aperto più vecchio: *«i PDF: aprirli davvero, uno per tool»*. In
questo ambiente non si possono guardare le pagine come immagini, ma si può fare
di meglio che fidarsi: **catturare il file che il browser sta per scaricare,
salvarlo su disco e rileggerne il testo dai flussi di contenuto**. È quello che
il professionista si ritrova in mano.

## Bilancio civilistico — prospetto di deposito

Catturato il PDF di «Scarica bilancio (PDF)»: 51.443 byte, due pagine. Riletto
tutto. È corretto, e vale la pena dire in che senso:

- schema abbreviato, art. 2435-bis c.c., con i dati anagrafici in testa;
- Stato patrimoniale attivo e passivo che quadrano a 687.200 su entrambi gli
  esercizi, Conto economico fino all'utile di 118.100;
- importi con il separatore delle migliaia e i negativi fra parentesi —
  `(12.000)` per gli oneri finanziari — che è la convenzione dei prospetti;
- numerazione delle voci IV Direttiva corretta, comprese le sottovoci
  (`5) altri ricavi e proventi` → `b) altri`);
- piede con «Alfa Industriale S.r.l. · Bilancio al 31-12-2025 · Pag. 1 di 2».

**Un falso allarme, il sesto della giornata.** Nel testo estratto sembrava
esserci un doppio spazio dove il sorgente ha un trattino lungo — «Schema
abbreviato␣␣art. 2435-bis». Sembrava che jsPDF perdesse i caratteri fuori dal
Latin-1. Guardando i byte veri: c'è `0x97`, che in `WinAnsiEncoding` **è** il
trattino lungo. Il terminale non lo mostrava. Nel PDF non manca niente: zero
stringhe a due byte, cioè nessun carattere è finito nella codifica di ripiego.

## Generatore F24 — modello ministeriale

Catturato il PDF di un F24: 604.802 byte, tre pagine (le copie del modello).
Il modello ministeriale è un'immagine da 2481×3508 — A4 a 300 dpi — **incorporata
una volta sola** e riusata, non una copia per pagina.

I valori sopra non sono disegnati: sono **testo vero**, quindi il PDF resta
cercabile e copiabile. Riletti dai flussi compressi: `ALFA S.R.L.`, `Milano`,
`MI`, `Via Roma 12`, codice tributo `1040` periodo `0008` anno `2026` importo
`5500`+`00`, il credito `6099` anno `2025` importo `2000`+`00`, i totali e il
saldo `+` `3500`+`00`. I centesimi stanno nelle caselle separate, come vuole il
modello.

## Generatore LIPE — prospetto di lavoro

333.844 byte, quattro pagine. Il modello ministeriale è un'immagine 1654×2339 —
A4 a 200 dpi — e i valori sopra sono testo. Su **ogni pagina** c'è la filigrana
«PROSPETTO DI LAVORO — NON PRESENTABILE», che è esattamente l'onestà che serve
per un modello che non è ancora passato dal software di controllo dell'Agenzia.

Gli importi sono scritti `115000,00`, senza separatore delle migliaia. Non è una
svista: è la forma delle caselle del modello, e il separatore lì non ci va.

## Fascicolo fiscale cliente — PDF impaginato

734.264 byte, quattro pagine, e qui la tecnologia è un'altra: **ogni pagina è
un'immagine a piena pagina** (1240×1754, A4 a 150 dpi; l'organigramma in
orizzontale). Sembrava che il fascicolo uscisse come una scansione — settimo
falso allarme della giornata. Non è così: sotto ogni immagine c'è il testo
completo dichiarato come `/ActualText` in uno span di contenuto marcato, cioè il
meccanismo di accessibilità del PDF. Decodificato: «Alfa Industriale S.r.l. ·
Fascicolo fiscale cliente · esercizio 2026 · Profilo permanente al 23/08/2026 ·
SINTESI · Attività: produttore, su mercati Italia, UE…».

Quindi uno screen reader lo legge e il testo si copia. Restano due cose da
sapere: la ricerca dentro il PDF dipende dal visualizzatore, perché il testo non
ha posizione per singolo glifo; e a 150 dpi la stampa è più morbida del testo
vettoriale degli altri tre tool. È una scelta diversa dal resto della suite, non
un difetto — ma è una scelta, e vale la pena saperla.

## Come si rifà

Lo strumento è in `tests/` solo di fatto, non ancora come test: si intercetta
`URL.createObjectURL`, si prende il Blob, lo si porta a base64 e lo si decodifica
su file; poi un estrattore di una cinquantina di righe legge i flussi (Flate) e
tira fuori le stringhe fra parentesi. Per i flussi compressi conviene tagliare
con `/Length` del dizionario invece di cercare `endstream`, che dentro
un'immagine capita per caso.

---

# Giro 6 — tasti e accessibilità sul telefono, 24 agosto 2026

Percorse **tutte** le sezioni dei sei tool alla larghezza di un telefono (375 px)
— 21 in Analisi, 16 nel Fascicolo, 8 in LIPE e nell'F24, 7 in Confronto regimi e
nel Bilancio — misurando ogni pulsante, ogni link e ogni campo.

## Il risultato buono, prima

**Nessun pulsante senza nome accessibile. Nessun campo senza etichetta. Nessuno
scorrimento orizzontale.** In nessuna schermata di nessun tool. L'unico elemento
minuscolo che resta è il salto al contenuto, che è 1×1 finché non lo si mette a
fuoco: è così che deve essere.

## Quattro bersagli troppo piccoli per un dito

Tutti alti 15 px o 22, e tutti per lo stesso motivo: un pulsante vestito da link
con `padding:0`.

| Dove | Prima | Ora |
|---|---|---|
| «Prova con i dati dimostrativi» — una delle tre strade d'ingresso, in quattro tool | 161×15 | 165×27 |
| «Modifica» sull'anagrafica dell'F24 | 51×15 | 27 di lato |
| «Apri» sulla riga dell'F24 da verificare | 24×15 | 27 di lato |
| «Apri», «Esporta», «Elimina» dell'archivio clienti di LIPE, **solo sul desktop** | 40×22 | 26 |
| I quattro link del piede dei tool: «Privacy · Sicurezza · Termini · Licenze» | 16 di altezza, 10 di distanza | 28 |

Il margine negativo compensa l'imbottitura, quindi non si è spostato niente: i
42 controlli responsive passano come prima, e il piede resta su una riga sola.

Il piede delle pagine editoriali è un'altra cosa — un `nav` con 19 px di
distanza e link già alti 32 — e non è stato toccato.

## E adesso lo misura l'armatura

I quattro li ho trovati a mano. Perché non tornino, `tests/responsive-harness.html`
ora cerca, per ogni tool e per ogni larghezza, il pulsante col lato più corto, e
fallisce sotto i 24 px. Il controllo vale a **ogni** larghezza, non solo sul
telefono: le WCAG non distinguono, ed è così che è saltato fuori il caso di LIPE,
che a 375 px non si vedeva perché lì la regola che ingrandisce quei pulsanti c'è
già.

Provato che serve: togliendo l'imbottitura a «Prova con i dati dimostrativi»,
**22 controlli su 42** diventano rossi e dicono quale pulsante e quanto misura.
Il bersaglio più piccolo di ogni tool sta ora fra 26 e 30 px.


---

# Giro 7 — gli output che legge la macchina, 24 agosto 2026

I PDF li avevo aperti nel giro 5. Restavano i due file che non guarda una
persona ma un sistema: l'istanza XBRL del Bilancio e l'XML IVP18 di LIPE. Sono
gli output dove un errore non si vede — li rifiuta l'Agenzia, davanti al
cliente.

## L'XBRL del Bilancio

Generato e riletto per intero. È ben formato, dichiara la tassonomia PCI del
4 novembre 2018 e contiene **108 fatti** su quattro contesti (esercizio corrente
e precedente, istante e durata). L'unità è una sola, EUR, e i numeri sono quelli
dello schermo: totale attivo e totale passivo tornano **entrambi a 687.200** su
tutti e due gli esercizi, l'utile è 118.100. **95 fatti monetari** hanno tutti
`unitRef` e `decimals`; i quattro che sembrano numeri ma non ce l'hanno sono
partita IVA, codice fiscale, numero REA e codice ATECO — che numeri non sono, e
infatti la tassonomia li vuole come stringhe. In testa l'istanza dichiara che
non contiene la nota integrativa, che è vero e va detto.

**Il difetto.** Il commento di intestazione diceva «Bilancio civilistico
v1.4.0». Il tool gira con 1.4.1. Un file destinato a un deposito che si presenta
con la versione sbagliata di chi l'ha prodotto è il tipo di dettaglio che nessuno
guarda finché non serve. Ora la versione la legge da `data-tool-version`, cioè
dallo stesso attributo che i test controllano: non può più restare indietro.

Cercati gli altri simili in tutti e sei i tool: **sei etichette** dichiaravano
1.4.0 nel Bilancio (it/en/es) e 3.5.0 in LIPE (it/en/es). Non si vedevano — un
blocco le riscrive dalla versione viva prima che compaiano — ma è proprio questo
il punto: aprendo il file, la risposta era il numero sbagliato. È esattamente il
modo in cui mi sono sbagliato più volte in questi due giorni. Allineate, e ora
un test le guarda: quello vecchio cercava `vX.Y.Z</`, e dentro le virgolette di
un dizionario non ci arrivava.

Sono rimasti fuori due letterali in Analisi (`'1.5.0'`, `'1.6.0'`): sono i
marcatori interni dei blocchi di correzione, non la versione del tool, e stanno
bene dove sono.

## L'XML IVP18 di LIPE

Ben formato, namespace dell'Agenzia, `CodiceFornitura IVP18`, tre moduli — uno
per mese del trimestre. L'aritmetica torna riga per riga: giugno ha esigibile
26.400 meno detratta 2.500 uguale dovuta 23.900, meno il credito del periodo
precedente 4.600 fa **19.300 da versare**. Gli importi sono in centesimi interi,
come vuole il tracciato, e il nome del file segue la convenzione —
`IT01234567890_LI_00002.xml` — col progressivo che avanza a ogni generazione.

**L'ottavo falso allarme di questo audit.** Sembrava che l'identificativo del
trasmittente fosse obbligatorio ma non finisse da nessuna parte nel file. Finisce
nel **nome** del file, che è dove l'Agenzia lo vuole. E il tool, nel frattempo,
si rifiuta di generare l'XML finché quel dato non c'è *ed è salvato*, e nel
messaggio distingue i due casi. Funzionava già come doveva.

## Cosa resta fuori da qui

Ben formato e coerente non vuol dire accettato: la prova definitiva è il
**modulo di controllo dell'Agenzia** per il file LIPE e **TEBENI** per l'XBRL.
Sono programmi da scaricare e far girare a mano, e restano l'unica cosa
dell'audit che non posso chiudere da qui.

---

# Giro 8 — Confronto regimi, e la domanda d'apertura, 24 agosto 2026

Restava il punto più vecchio dell'elenco: **«Da dove vuoi partire?» mancava in
Fascicolo e in Confronto regimi**. L'avevo lasciato lì perché è un cambio di
forma, non una correzione. Guardandolo davvero, i due casi si sono rivelati
diversi.

## Confronto regimi: le tre strade erano in fondo

Aprendo il tool si vedono tre gruppi di scelte — professione, regime attuale,
anno e soggetto — e **solo dopo averli scorsi tutti**, in fondo, una fila di
quattro pulsanti: continua, prova i dati dimostrativi, scarica il template,
importa da Excel. Misurato: il primo pulsante stava a 788 px dall'alto, cioè
sotto la piega su qualunque schermo.

Vuol dire che chi apre il tool per capire cosa fa non sa che c'è un esempio da
provare finché non ha già compilato il profilo. E che l'azione principale
(«Continua con i compensi») stava nella stessa fila di tre alternative, tutte
dello stesso peso visivo.

Ora il blocco condiviso è **in testa alla prima tappa**, con le tre strade vere:

- **Importa da Excel** — il template compilato con l'AI;
- **Inizia a mano** — porta il fuoco sulla prima scelta, che è anche un salto
  utile per chi naviga da tastiera;
- **Riprendi una simulazione** — apre l'archivio premendo la stessa voce di menu
  che c'è già, così non esistono due strade che fanno la stessa cosa in due modi.

Sotto, il pulsante dei dati dimostrativi. In fondo alla tappa restano due
pulsanti: l'azione principale e il download del template, che sta col file.

Verificato con il tool aperto: le tre schede rispondono, l'esempio carica e
salta direttamente al confronto (compensi 120.000, «Studio dimostrativo»),
l'archivio si apre su `#/archivio`. Su 390 px le schede si impilano a tutta
larghezza, niente scorrimento orizzontale, e il bersaglio più piccolo della
pagina è ora il pulsante dell'esempio, 27 px. L'armatura: **42 controlli su 42**.

**Un nono falso allarme, evitato.** La prima misura diceva che «Inizia a mano»
*selezionava* la professione — un difetto che avrei introdotto io. Non era vero:
il tool ripristina il lavoro salvato in questo browser, e quel radio era già
selezionato prima del clic. Azzerati i due radio a mano, dopo il clic restano
entrambi vuoti e il fuoco è sul primo. `focus()` non seleziona.

Versione portata a **1.4.1**, allineata in tutti e sei i posti che la
dichiarano.

## Fascicolo: il blocco non ci va

Nel Fascicolo la prima cosa che si vede è l'elenco dei clienti, con «＋ Nuovo
cliente» e «Importa Excel» in testa, sopra la piega. **L'archivio è già la
schermata d'ingresso**: aggiungere un blocco che chiede da dove partire vorrebbe
dire chiedere due volte la stessa cosa. Non l'ho messo.

Anche qui una verifica ha smontato un difetto che sembravo aver trovato: lo
stato vuoto dice «Nessun cliente trovato — modifica la ricerca», che sarebbe
sbagliato se l'archivio fosse davvero vuoto. Ma vuoto non può essere: il
pulsante di eliminazione compare solo quando i clienti sono più di uno.

## Una parola sola per la stessa domanda

F24 apriva con «Tre modi per cominciare», gli altri quattro con «Da dove vuoi
partire?». Ora la domanda è la stessa in tutti e cinque: è una domanda a chi
legge, e non va riscritta quando cambia il numero delle schede.

E in `CONTESTO.md` l'elenco delle versioni era rimasto indietro — diceva `3.6.0`
per LIPE e non nominava Confronto regimi.

---

# Giro 9 — il software dell'Agenzia, e i file da dargli in pasto, 24 agosto 2026

Il pacchetto **Desktop Telematico 1.4.0** è arrivato come `.zip` da 170 MB che
non si apriva. L'ho estratto: 253 MB, 1.812 file, 244 plugin, con una **JRE già
dentro** — non serve installare Java. Ora sta in `C:\Users\RiccardoZedda\
DesktopTelematico`, si avvia con `DesktopTelematico.exe`, e la cartella si può
cancellare quando non serve più.

**Cosa c'è e cosa manca.** Dentro c'è l'*impalcatura* dei controlli
(`it.sogei.rcp.controlli.definizioni`, l'interfaccia `IClasseControllo` e il
punto di estensione), non i moduli veri. I moduli per singola dichiarazione —
F24A0, IVP18 — si installano **dall'applicazione**, dalla voce che il pacchetto
chiama `launchInstallSoftware`. Nel pacchetto non c'è nessun indirizzo di
aggiornamento preconfigurato: quelli che si trovano nei file di p2 sono resti
della macchina che ha costruito il pacchetto. Quindi il controllo offline, da
qui, non si può fare: va fatto aprendo il programma.

**Ho preparato i file.** In `DesktopTelematico\da-controllare` ci sono i due
output veri, generati dai tool e non scritti a mano:

- `IT12345678903_LI_00001.xml` — la comunicazione LIPE, 2.440 byte, ben formata;
- `F24_Diretti_2026-09-16.zip` — due tracciati F24A0 (una cartella per
  contribuente) più il CSV di riepilogo, già estratti accanto.

## Quello che ho trovato preparandoli

**L'esempio di LIPE non arrivava all'XML.** Il dato «identificativo del soggetto
trasmittente» è obbligatorio e il tool si ferma prima di generare, con un
messaggio corretto — ma l'esempio dimostrativo non lo compilava. Vuol dire che
chi provava il tool con i dati finti non vedeva mai l'output principale.
Adesso l'esempio lo porta: è un invio in proprio, quindi il trasmittente è la
partita IVA del contribuente. Un clic, e l'XML esce.

**In F24 il pulsante spento diceva la cosa sbagliata.** Il file telematico si
genera solo dopo aver spuntato «controllerò il file con il software
dell'Agenzia». L'avevo spuntata, e il pulsante restava spento: mancavano gli
IBAN di addebito, che l'esempio non ha. Il pannello lo diceva — «4 dati da
completare», con l'elenco — ma la riga **accanto al pulsante** diceva «Dopo il
download: Desktop Telematico → …», cioè descriveva un passo successivo a un
download che non poteva partire. Ora quella riga dice quanti dati mancano finché
ne manca uno.

**Un decimo e un undicesimo falso allarme.** Il pulsante che «non faceva niente»
era disabilitato, non rotto. E il pannello che «non spiegava perché» lo
spiegava, in un blocco a scomparsa poco sopra.

## Una domanda che il modulo di controllo deve sciogliere

I record F24A0 prodotti sono **1.898 caratteri più CR-LF**, cioè 1.900 byte per
riga. Il tool lo impone e lo verifica. La frase in pagina però diceva «record a
lunghezza fissa di 1.900 caratteri», che descrive il file solo se nei 1.900 si
contano i due caratteri di fine riga. L'ho resa esatta — «1.900 byte l'uno,
CR-LF compreso» — ma **la domanda vera resta**: il tracciato vuole 1.900
posizioni *più* il fine riga, o 1.900 comprese? È la prima cosa da chiedere al
modulo F24A0, ed è per questo che il tool marca ancora quel tracciato come «Da
validare».

## Contorno

Alzate le versioni: LIPE **3.6.2**, F24 **1.0.1**, allineate ovunque.

E un controllo che si è messo di traverso: `tests/audit-remediation.test.mjs`
cercava `data-tool-version="3.6.1"` alla lettera, e ha bocciato un avanzamento
di versione legittimo. Il numero non ci va scritto: ora legge l'attributo del
tag e verifica che le altre quattro dichiarazioni di LIPE dicano la stessa cosa.
Provato che fallisce ancora, disallineando una costante.

---

# Giro 10 — la tastiera, 24 agosto 2026

Il giro 6 aveva misurato i tasti col dito. Restava chi il dito non lo usa: la
tastiera. Ho aperto le finestre di dialogo dei sei tool e ho premuto Tab.

## Il tabulatore usciva dalla finestra

**In nessuno dei sei tool la finestra tratteneva il fuoco.** Aperta la finestra
«Nuova anagrafica» in F24, dietro restavano **26 elementi raggiungibili** con
Tab: si continuava a girare fra pulsanti che non si vedono, dentro una pagina
oscurata. Chi usa il mouse non se ne accorge mai. Chi naviga da tastiera si
perde ogni volta, e per uscire deve indovinare.

E alla chiusura il fuoco **cadeva sul `body`**: il Tab successivo ripartiva
dall'inizio della pagina, dal salto al contenuto. Non perché il tool sbagliasse
a restituirlo, ma perché il pulsante che aveva aperto la finestra nel frattempo
era sparito — è dentro una tappa che non si vede più.

## La correzione non sta dentro i tool

Le finestre dei sei tool sono scritte in sei modi diversi: chi con un involucro
`.modal-ov`, chi con `hidden`, chi disegnandole a runtime. Correggerle una per
una avrebbe voluto dire sei correzioni da tenere allineate per sempre.

`assets/tal-focus-guard.js` non conosce nessuno dei sei. Guarda solo se in
pagina c'è un elemento con `role="dialog"` visibile, e allora:

1. **tiene il tabulatore dentro, in cerchio** — Tab dall'ultimo torna al primo,
   Shift+Tab dal primo va all'ultimo, e se il fuoco è fuori lo riporta dentro;
2. **dà un nome alla finestra** se non ce l'ha, prendendolo dalla sua
   intestazione: le due del Bilancio ne erano prive, e un lettore di schermo
   annunciava «finestra di dialogo» e basta. Ora dice «Memorie salvate»;
3. **alla chiusura rimette il fuoco** sul pulsante che l'aveva aperta, o — se
   quel pulsante non c'è più — all'inizio del contenuto, non della pagina.

Il terzo punto interviene **solo se il fuoco si è perso davvero**. Il Fascicolo
lo restituisce da sé, e lì la guardia non tocca niente: verificato, dopo
l'Escape il fuoco è ancora su «Cambia cliente».

**Una trappola di misura, la dodicesima.** La prima lettura diceva che tutte le
finestre di F24 erano prive di `role` e di nome. Non è vero: ruolo, `aria-modal`
e nome stanno sull'**involucro** `.modal-ov`, non sul riquadro `.modal` che
guardavo io. E il mio rilevatore di visibilità usava `offsetParent`, che per un
elemento `position:fixed` è **sempre** `null`: mi diceva che nessuna finestra
era aperta mentre ne avevo una davanti. La visibilità va misurata col rettangolo
e con lo stile calcolato.

Una finestra senza nome è rimasta, ed era vera: l'anteprima del modello in F24
non ha intestazione da cui prenderlo. Gliel'ho scritto a mano.

## Come si tiene

`tests/finestre.test.mjs`: la guardia si compila, tutti e sei i tool la
caricano, e ogni finestra dichiarata nel sorgente ha ruolo, `aria-modal` e nome.
Provato che fallisce togliendo il tag da LIPE. **120 test.**

---

# Giro 11 — i collegamenti, 24 agosto 2026

Sessantasette pagine, e nessuno aveva mai controllato che i link fra loro
portassero da qualche parte. Non serve un server per farlo: le pagine sono file,
e `/qualcosa/` è `/qualcosa/index.html`. Ho scritto il controllo e l'ho passato
su tutto il sito.

**Collegamenti rotti: zero.** Nessun file mancante, nessuna pagina indicizzabile
mai collegata da nessun'altra. Su un sito costruito a mano in due lingue in più,
non era scontato.

**Ancore che non portavano da nessuna parte: trentanove.**

## `#about` esisteva solo in italiano

L'intestazione di ogni pagina ha la voce «Chi sono» / «About» / «Sobre mí», e
punta a `/#about`, `/en/#about`, `/es/#about`. In italiano quella sezione ha
`id="about"` — con sopra un commento che spiega che il link non funzionava e che
è stato aggiunto apposta. **Nelle home inglese e spagnola l'`id` non è mai
arrivato**: 19 pagine per lingua puntavano a un bersaglio inesistente.

Poi l'ho misurato, e la conclusione è più stretta di come suonava.

La home è un'applicazione a due viste: un instradatore JavaScript legge la parte
dopo il `#` e mostra la vista giusta. Quindi **con il mouse la voce funzionava
lo stesso**: si arrivava alla pagina «About», col titolo giusto nella scheda.
Quello che non funzionava è **dove finisce il fuoco**. Misurato prima e dopo,
sulla stessa pagina:

- senza `id`: la vista cambia, ma il fuoco resta sul `body`. Chi naviga da
  tastiera arriva su «About» e il Tab successivo riparte dal salto al contenuto,
  in cima al documento;
- con `id="about" tabindex="-1"`: il fuoco si posa sulla sezione, e il Tab
  successivo continua dentro la pagina che si è appena aperta.

È lo stesso difetto del giro 10, in un altro punto: la vista si sposta e il
fuoco resta indietro.

## La scheda di LIPE non aveva il suo indirizzo

In «Configura con AI» ogni tool ha la sua scheda con un `id`, e ogni tool ci
punta dal proprio pulsante. **LIPE era l'unica delle sei senza `id`**: il
collegamento `/configura-con-ai/#lipe`, scritto nel tool, non aveva bersaglio.
Verificato in pagina, non nel sorgente — quelle schede in parte nascono a
runtime, ed è la trappola in cui ero già caduto nel giro 4. Adesso c'è.

## Il controllo che si dava ragione da solo

`tests/collegamenti.test.mjs` tiene tutto questo. La sua **prima versione era
sbagliata**: raccoglieva gli `id` da tutto il file, commenti compresi. E il
commento che spiega la correzione contiene, per forza, la stringa `id="about"`.
Risultato: ho tolto l'attributo vero per provare che il controllo fallisse, e
quello ha continuato a dire che andava tutto bene.

Ora commenti e script vengono tolti prima di leggere gli `id`. Provato di nuovo:
adesso fallisce, e dice quale pagina e quale ancora. **123 test.**

**Tredicesima trappola di misura, e vale la pena scriverla per intero.** Un
controllo dei collegamenti dice «questa ancora non esiste», non «questo link non
funziona». Su una pagina con un instradatore JavaScript le due cose sono
diverse, e se avessi riportato la prima come se fosse la seconda avrei scritto
un difetto più grave di quello vero. Il difetto vero c'era — ma era sul fuoco,
non sulla navigazione.

E una cosa che qui non si può misurare per niente: **in questo ambiente la
pagina non scorre**. Né con `location.hash`, né con `scrollTo`, né con
`scrollIntoView`: il riquadro del browser non sta disegnando, e `scrollY` resta
zero comunque. Qualunque affermazione sullo scorrimento, in questo giro, sarebbe
stata inventata.

## Contorno

In `CONTESTO.md` la sezione di riferimento si intitolava ancora «I cinque tool»
e non nominava Confronto regimi.

---

# Giro 12 — quello che vede un motore di ricerca, 24 agosto 2026

Il sito parla tre lingue e ha 45 pagine indicizzabili. Le dichiarazioni che le
tengono insieme — `canonical`, `hreflang`, sitemap, dati strutturati — non si
vedono in pagina, e quando sbagliano fanno danno in silenzio: due lingue che si
dichiarano l'una il duplicato dell'altra, una pagina `noindex` proposta come
alternativa, un articolo che si presenta col titolo di un altro. Nessuno le
aveva mai lette.

## Le tre lingue: tutto in ordine

Misurate pagina per pagina, sono **corrette tutte e quarantacinque**:

- ogni pagina indicizzabile ha un `canonical` che punta a sé stessa;
- gli `hreflang` **si ricambiano**: se l'italiana indica l'inglese, l'inglese
  indica l'italiana, e con lo stesso indirizzo;
- `x-default` c'è ovunque;
- nessun `hreflang` propone una pagina `noindex` — le 22 pagine di avviso e i
  rimandi non ne dichiarano nessuno, che è la cosa giusta;
- la sitemap elenca **45 URL: esattamente le 45 pagine indicizzabili**, nessuna
  in più, nessuna in meno.

Non c'era niente da correggere. Vale la pena scriverlo lo stesso: è la parte del
sito dove un errore non si vede finché non si guardano le statistiche di Google
tre mesi dopo.

**Quattordicesimo falso allarme.** La prima misura diceva che il Fascicolo non
aveva il `canonical`. Ce l'ha: è scritto `<link href="…" rel="canonical"/>`,
con gli attributi nell'ordine opposto agli altri cinque, e il mio controllo li
cercava in un ordine solo.

## I dati strutturati: due cose vere

Trentacinque blocchi JSON-LD, tutti JSON valido. Due difetti.

**Analisi di bilancio era l'unico dei sei tool senza dati strutturati.** Zero
blocchi — verificato in pagina, non nel sorgente. Gli altri cinque dichiarano
ciascuno la propria `SoftwareApplication` con nome, descrizione, versione,
licenza, autore e prezzo zero; Analisi no, quindi non poteva comparire come
applicazione nei risultati di ricerca mentre i suoi cinque fratelli sì. Ora ce
l'ha, modellata sulla loro, con la versione che i test tengono allineata.

**Un articolo spagnolo si presentava col titolo di prima.** «Análisis y
reclasificación financiera de gestión» nella `headline`, mentre il titolo e
l'H1 dicono «Análisis financiero y reclasificación de gestión». È la stessa
famiglia di difetti del giro 4 — un titolo corretto in un posto e non
nell'altro — e questa volta stava nei dati che legge la macchina.

**Quindicesimo falso allarme, e istruttivo.** La prima versione del controllo
confrontava la `headline` con il `<title>` e trovava **nove** difetti. Non ce
n'era nemmeno uno: la `headline` va confrontata con l'**H1**, e con l'H1
tornava. Il `<title>` è volutamente più corto e scritto per i motori di ricerca
— è la stessa scelta deliberata che avevo già riconosciuto nel giro 4 per le
descrizioni social, e che stavo per contare come deriva.

## Come si tiene

Due file nuovi, **131 test**:

- `tests/lingue.test.mjs` — canonical, hreflang reciproci, niente hreflang sulle
  pagine `noindex`, sitemap uguale all'elenco delle indicizzabili;
- `tests/dati-strutturati.test.mjs` — JSON valido, ogni tool con la sua
  `SoftwareApplication` completa e con la versione che gira, `headline` uguale
  all'H1, indirizzi e immagini che esistono davvero.

Entrambi provati rompendoli: tolto il `canonical` dalla home inglese, e messa
una versione sbagliata nei dati di Analisi.

---

# Giro 13 — gli Excel, aperti uno per uno, 24 agosto 2026

I PDF li avevo aperti nel giro 5, gli XML nel giro 7. Restavano le cartelle
Excel: quelle che il professionista archivia e riapre fra sei mesi. Le ho
prodotte e rilette dal file finito — intercettando il Blob e rileggendolo con
`XLSX.read`, non guardando il codice che lo scrive.

## Le schede erano tagliate a metà parola

Il working paper di Analisi ha una scheda per ogni riclassificazione. Uscivano
così:

```
1_Stato patrimoniale — criterio
2_Stato patrimoniale — pertinen
3_Capitale investito e posizion
4_Conto economico — valore aggi
5_Conto economico — EBITDA   EB
6_Conto economico — aree gestio
```

Tutte esattamente 31 caratteri, che è il massimo che Excel accetta. Il nome
veniva tagliato e basta. E la cosa che lo rende un difetto vero e non un
dettaglio: **dentro il foglio quel titolo non c'è** — la prima cella dice
«Voce». Il nome della scheda è l'unico posto del file in cui compare, e diceva
«3_Capitale investito e posizion».

La quinta aveva anche tre spazi in mezzo: i caratteri che Excel vieta (`/`,
`\`, `?`, `*`, `[`, `]`, `:`) venivano sostituiti con uno spazio senza
ricompattare, e «EBITDA / EBIT» diventava «EBITDA   EB».

Ora le schede si chiamano:

```
1_SP — criterio finanziario        4_CE — valore aggiunto
2_SP — pertinenza gestionale       5_CE — EBITDA EBIT
3_Capitale investito e PFN         6_CE — aree gestionali
```

Le sigle sono quelle del mestiere e si applicano **sempre**, non solo quando il
nome sfora: nella prima versione della correzione la scheda 5 ci stava per un
carattere e restava «Conto economico», mentre la 4 e la 6 diventavano «CE» —
tre schede affiancate, due modi di scrivere la stessa cosa. Il taglio, se
proprio serve, ora cade su uno spazio.

**La trappola numero uno, di nuovo.** Avevo corretto `safeSheet` e riaperto il
file: schede identiche. Il working paper vivo non passa da lì — lo costruisce il
blocco v20, che aveva il suo `v20SheetName`, con la stessa riga sbagliata
copiata. Me ne sono accorto **solo** perché ho riaperto il file prodotto invece
di fidarmi di aver modificato «la funzione». Ora `v20SheetName` delega a
`safeSheet`: una regola sola, in un posto solo.

## Lo stesso foglio KPI usciva con due tracciati

I 42 indicatori si possono esportare da due pulsanti. Uscivano diversi:

| | KPI Excel | dentro il working paper |
|---|---|---|
| terza colonna | `KPI` | `Indicatore` |
| descrizione | assente | `Descrizione operativa` |
| ottava/nona | `Nota`, `Segnalazione` | `Note`, **niente segnalazione** |

Stessi numeri, due forme. Chi mette i due file affiancati non trova le colonne
allineate; e la **segnalazione** — l'avviso che dice quando un indicatore poggia
su un dato incerto — spariva proprio dal file più completo.

Adesso il tracciato è uno solo, l'unione dei due, in entrambi i punti:

```
Codice · Famiglia · Indicatore · Descrizione operativa · Valore · Unità ·
Formula · Affidabilità · Nota · Segnalazione
```

Verificato sul file: intestazioni identiche, larghezze identiche, 42 righe in
tutti e due, e i formati numerici ancora al posto giusto (`0.00"x"` sul Current
ratio, che ora sta in colonna E invece che in D). Perché non torni a spostarsi,
**la formattazione ora trova le colonne leggendo l'intestazione invece di
contarle**: era proprio il contare che legava le due esportazioni allo stesso
numero di colonne.

## Quello che ho verificato e NON era un difetto

- **Il foglio «Esiti AI»** non è un residuo da ripulire: si chiama così perché
  quello è il nome del foglio nel template, il tool lo legge con quel nome e il
  messaggio di errore lo cita. Rinominarlo in uscita lo scollegherebbe
  dall'ingresso.
- **Il Bilancio che «non esportava»**: il pulsante risponde con «Conferma la
  voce tecnica A.IX nella scheda Verifiche prima di esportare». È un blocco
  voluto, e dice esattamente cosa fare.
- **Le larghezze di colonna che sembravano sparite**: rileggendo il file senza
  `cellStyles: true` SheetJS non le restituisce. Ci sono.
- **I nomi delle schede negli altri cinque tool**: cercati tutti, nessuno
  arriva a 28 caratteri. Il problema era solo di Analisi.

## Una cosa che resta

**Analisi di bilancio dichiara la propria versione in trentasei punti** —
`data-tool-version`, i dati strutturati, l'etichetta, e una costante in ognuno
dei dodici blocchi di correzione sovrapposti. Il Bilancio ha una costante sola:
è una correzione già fatta lì e mai portata qui. Per questo non ho alzato la
versione in questo giro: farlo bene significa consolidare prima le trentasei
dichiarazioni, ed è un intervento a sé, non la coda di una correzione sugli
Excel.

---

# Giro 14 — i titoli neri sui banner scuri, 24 agosto 2026

Segnalato da Riccardo guardando la pagina: nelle riclassificazioni di Analisi il
titolo della tabella è **nero su fondo petrolio**. Misurato: rapporto di
contrasto **1,07**. Il minimo leggibile è 3,0; sotto quel valore non è questione
di dimensione, non si legge e basta.

La cosa che lo rende istruttivo: l'occhiello accanto — «STATO PATRIMONIALE», in
alto a destra nella stessa fascia — era **bianco**. Due testi nella stessa
barra, uno leggibile e uno no.

## Perché una riga di CSS scritta bene non faceva niente

`assets/tal-app.css` impone a ogni titolo del guscio:

```css
html.tal-tool-page h2:not(:is(header.top, footer.foot, …) *){
  color: var(--tal-fg) !important;
}
```

Analisi, dal primo giorno, dichiarava `.reclass-head h2{color:#fff}`. Perde:
l'`!important` del guscio vince, e il titolo torna nero. L'occhiello si salvava
perché è un `<small>`, e la regola tocca solo `h1`, `h2` e `h3`.

Corretto aggiungendo `.reclass-head` — e una classe generica `.tal-on-dark`, così
un banner scuro nuovo può dichiararsi senza tornare a toccare quel file — alla
lista di eccezioni di tutte e tre le regole. Verificato in pagina: i sei titoli
sono bianchi, rapporto fra **9,57 e 13,41**. E si sono sistemati insieme a loro
altri quattro banner della stessa famiglia — i flussi A, B, C e la
riconciliazione del rendiconto finanziario — che avevano lo stesso difetto e che
nessuno aveva segnalato.

## Il controllo che l'avrebbe visto, e che non c'era

Scritto `tests/banner-scuri.test.mjs`, che è una regola di coerenza più che di
grafica: **se un tool dichiara un titolo chiaro dentro un contenitore, quel
contenitore deve comparire fra le eccezioni del guscio; altrimenti quella
dichiarazione è morta.**

Alla prima esecuzione ha trovato subito un secondo caso che non era stato
segnalato: `.advanced-head h2{color:#fff}`, i banner di DuPont, dei bridge e
della serie storica. Stesso difetto, stessa causa. Corretto e verificato in
pagina.

Provato che fallisce togliendo `.reclass-head` dalle eccezioni. **138 test.**

## L'armatura del contrasto non poteva vederlo, e ora si sa perché

`tests/contrast-harness.html` girava da giorni dicendo «nessun problema».
Rilanciata dopo la segnalazione, continuava a dirlo. Due limiti, tutti e due
scritti nel file ma nessuno dei due abbastanza in vista:

1. **Dichiarava «non misurabile» ogni fondo a gradiente.** Era prudenza: al primo
   giro segnalava 90 problemi e 88 erano l'intestazione e la sidebar, che hanno
   fondo a gradiente e testo chiaro perfettamente leggibile. Ora i gradienti
   **fra colori pieni** si misurano, prendendo la fermata che dà il rapporto
   peggiore: il testo deve reggere su tutta la larghezza del banner, non solo
   dove il fondo aiuta.
2. **Misura solo la schermata d'ingresso di ogni tool.** Le riclassificazioni
   stanno in una vista interna, dove l'armatura non entrava mai.

Ho provato a farle percorrere tutte le viste cliccando le voci del menu. Non
regge: novanta scansioni complete di pagine da 1–2 MB, e dopo due minuti era
ancora sul secondo tool. L'ho tolta. La copertura la dà il controllo statico sul
CSS, che costa **millisecondi** e coglie esattamente la stessa famiglia di
difetti — con il vantaggio di dire *perché* invece di dire *dove*.

**Sedicesimo falso allarme, per un soffio.** Con i gradienti misurati alla
lettera, l'armatura riportava **51** problemi, quarantacinque dei quali erano la
sidebar: i suoi fondi sono radiali `rgba(63,192,221,.3)` e `rgba(9,38,49,.62)`,
cioè **velature**, e prendendo le fermate senza guardare l'opacità risultava
ciano chiaro. Un gradiente translucido dipende dalla geometria e resta non
misurabile — dichiararlo è più onesto che dare un numero inventato.

---

# Giro 15 — gli export come documenti, 24 agosto 2026

Segnalato da Riccardo: nel PDF delle riclassificazioni finiscono frasi come
«Apri una voce per vedere i conti sottostanti. Le modifiche sono salvate per
società e metodologia.» Un export non è la pagina stampata: è un documento che
finisce in mano a qualcun altro, e le istruzioni per usare il tool lì dentro
non ci vanno.

## Le istruzioni fuori, il metodo dentro

Sei sezioni, sei note. Aperto il documento prodotto e contate: **quattro sono
istruzioni per l'interfaccia**, due sono metodo.

| nota | cosa è |
|---|---|
| «Apri una voce per vedere i conti sottostanti. Trascina una quota su un'altra voce per riclassificarla.» | istruzione |
| «Apri una voce… Le modifiche sono salvate per società e metodologia.» ×3 | istruzione |
| «Le poste finanziarie e non operative sono escluse dal capitale investito operativo.» | **metodo** |
| «PFN positiva = indebitamento netto; PFN negativa = cassa netta.» | **metodo** |

Non si buttano tutte: le due di metodo servono a chi legge il documento, e
toglierle renderebbe il prospetto meno leggibile, non più pulito. Il filtro
lavora **per frase**: butta quelle che parlano dell'interfaccia, tiene le altre.
Su una nota mista tiene solo la parte buona.

Via anche l'ultima riga del PDF: «Stampa browser: verificare anteprima, colori e
interruzioni di pagina prima del salvataggio PDF.» È un promemoria per chi
esporta, non un contenuto per chi riceve.

**Applicato a tutti e quattro i costruttori di stampa**, non solo a quello vivo.
Gli altri tre sono codice morto oggi — vale l'ultima definizione — ma «morto
oggi» non basta: basta che qualcuno riordini i blocchi e quella frase torna
dentro un PDF che va al cliente. Il filtro si espone su `window` apposta,
perché le tre chiamate vecchie stanno in IIFE diversi.

## Aria fra le sezioni

Il titolo di una sezione stava a 24 px dalla tabella precedente e la tabella
chiudeva a 18: le tematiche si toccavano. Ora ogni sezione ha 30 px sotto e 34
fra l'una e l'altra, il titolo respira dentro la sua fascia (padding da 9 a
11/14 px) e **le righe non si spezzano più a metà** fra due pagine
(`break-inside: avoid`).

## Un colore per tematica

Le fasce erano tutte dello stesso colore — un viola fisso nel foglio di stampa,
e a schermo un petrolio unico per stato patrimoniale e conto economico. In un
documento che contiene stato patrimoniale, conto economico e posizione
finanziaria, non si vedeva dove finisce una tematica e comincia l'altra.

Adesso sono quattro, **le stesse a schermo e nel PDF**, ricavate dal titolo con
la stessa regola:

| tematica | fascia |
|---|---|
| Stato patrimoniale | petrolio scuro `#0C3341` |
| Conto economico | petrolio chiaro `#1D6A7F` |
| Capitale investito e PFN | verde `#244C45` |
| Normalizzazioni | ambra `#704116` |

Resta il petrolio del marchio — la regola «un solo accento» scritta in
`tal-app.css` non è stata rotta: cambiano le tonalità dentro la stessa famiglia,
e le due tematiche che avevano già un colore proprio restano come stavano. Il
titolo bianco regge su tutte e quattro: contrasto misurato fra **5,5 e 9,1**.

## Come si tiene

`tests/export-pulito.test.mjs` **esegue** il filtro estratto dal file — non lo
guarda — e verifica che le tre frasi note spariscano, che le tre di metodo
restino, e che su una nota mista resti solo il metodo. Più due controlli: che
nessun costruttore di stampa scriva la nota grezza, e che negli altri cinque
tool quelle frasi non compaiano affatto. Provato che fallisce alterando il
filtro. **145 test.**

**Verificato negli altri cinque tool**: nessuno ha questo problema. Bilancio,
LIPE e F24 costruiscono i PDF con jsPDF, riga per riga, e non c'è nessun testo
d'interfaccia da portarsi dietro; Fascicolo e Confronto regimi non aprono
finestre di stampa.

---

# Giro 16 — le cartelle Excel come documenti, 24 agosto 2026

Stessa regola del giro 15, applicata al lato Excel: **esporre e riutilizzare
l'output**. Aperte le cartelle prodotte, non il codice che le scrive.

## Il triangolino dello schermo finiva nelle celle

Nel working paper ogni voce di primo livello usciva come **«▸Attivo
immobilizzato»**. Quel segno dice «questa riga si apre»: è un'affordance dello
schermo, e in una cartella Excel non si apre niente. Peggio: rompe la ricerca —
chi cerca «Attivo immobilizzato» non lo trova — e i confronti fra file.

Nel PDF veniva stampato davanti a ogni voce.

Tolto in tutti e due. La causa è la stessa: **Analisi è l'unico dei sei tool che
costruisce gli export leggendo la pagina** invece che lo stato, quindi si porta
dietro quello che la pagina ha in più. Verificato negli altri cinque: nessuno lo
fa, e nessuno ha glifi nelle celle.

## Ogni foglio comincia dal titolo

Le schede cominciavano dall'intestazione delle colonne. Il titolo per esteso non
c'era **da nessuna parte**: il nome della scheda è abbreviato per stare nei 31
caratteri di Excel, e chi apriva `3_Capitale investito e PFN` doveva indovinare
il resto. Ora la prima riga porta il titolo intero, unita per tutta la larghezza,
e sotto la nota di metodo quando c'è — passata dallo stesso filtro del PDF, così
le istruzioni per usare il tool non rientrano dalla finestra.

## Il filtro automatico, su tutte e sei le cartelle

È la cosa che di un prospetto si usa davvero: ordinare, filtrare, copiare via.
Messo nella **guardia condivisa**, così vale per tutti e sei i tool con una
regola sola, ed è deliberatamente prudente: intestazione fatta solo di testo,
**almeno tre colonne**, almeno quattro righe di dati.

Le tre colonne sono una correzione di rotta: con due bastava, e il filtro
compariva anche su «Parametri usati» e «Sintesi», che sono elenchi
chiave/valore, non tabelle. Misurato dopo: in Confronto regimi il filtro resta
su Costi e Assunzioni e sparisce dagli altri cinque fogli; in LIPE arriva su
Importi, Codiciario, Quadro VP, Verifiche e Dettaglio righi, e salta
«Informazioni» (due colonne) ed «Esiti AI» (una riga sola).

## E una scoperta che cambia cosa si può promettere

Aperto `xl/styles.xml` di una cartella scaricata: **due riempimenti — quelli di
sistema — e un solo carattere**. La copia di SheetJS che incorporiamo non scrive
gli stili.

Vuol dire che **non si vedono nel file**: le dodici assegnazioni di `cell.s` in
Analisi (l'intestazione scura, la scala di affidabilità dei KPI) e le tredici
`!freeze` fra LIPE, F24 e Confronto regimi. Non è un difetto dei tool — è la
libreria. Misurato eseguendola davvero:

| proprietà | arriva nel file? |
|---|---|
| formati numerici, larghezze, celle unite, filtro | **sì** |
| stili di cella, blocco riquadri | **no** |

Quindi la richiesta di **colori per tematica** si può soddisfare nel PDF, dove
l'ho fatto, ma **non** nella cartella Excel. Lì la differenza la fanno il titolo
in testa, il nome della scheda e il filtro. Per avere anche i colori bisogna
cambiare la build di SheetJS — è una decisione da prendere, perché quella
libreria ha una firma pubblicata nei notices e un test che la controlla.

`tests/excel-cosa-arriva.test.mjs` **estrae la libreria dal tool e la esegue**:
scrive una cartella con tutte quelle proprietà, la rilegge e dice cosa è
sopravvissuto. Il giorno in cui si cambia build, il test lo dice da solo — e
allora quelle righe di stile, che restano nel codice perché sono corrette,
cominciano a vedersi.

---

# Giro 17 — una sola versione per Analisi, 24 agosto 2026

Il punto aperto del giro 13. Analisi di bilancio dichiarava la propria versione
in **trentasei punti**: l'attributo sul tag, i dati strutturati, l'etichetta,
due `toolVersion` nei salvataggi, le stringhe «versione 2.1.0» dei report, il
nome del template scaricabile e **una costante in ognuno dei dodici blocchi di
correzione sovrapposti**.

Alzarla voleva dire trentasei sostituzioni, alcune dentro prosa storica
(«retained inside Financial Analysis 2.1.0») che va lasciata com'è. Per questo
nel giro 13 non l'avevo alzata: sarebbe stata la coda sbagliata di una
correzione sugli Excel.

## La sorgente è una

Un blocco in testa alla testata legge `data-tool-version` dal tag `html` — cioè
**quello che i test già controllano** — e lo mette in `window.FA_VERSION`. Le
dodici costanti dei blocchi ci puntano, e con loro i titoli dei report, il nome
del template scaricabile e la riga di testa della Dashboard dentro l'Excel.

È la soluzione che il Bilancio ha da tempo con `FS_VERSION`, con una differenza:
lì il numero è scritto nella costante, qui si legge dall'attributo. Una
ripetizione in meno.

## Provato alzandola

Non basta dire che il refactor è fatto: la prova è che il lavoro successivo
diventi facile. Ho portato Analisi a **2.1.1** toccando **solo i sei posti
statici** — attributo, JSON-LD, etichetta di pubblicazione, `manifest.json` e i
tre badge dei cataloghi — esattamente come per gli altri cinque tool.

In pagina si sono mossi da soli: la costante (`V20_VERSION` dice 2.1.1),
l'etichetta, e il titolo del foglio Dashboard nella cartella Excel. Le viste si
aprono tutte, l'export produce i dieci fogli. Nel corpo della pagina restano
nove occorrenze di «2.1.0»: sono tutte dentro commenti storici, nessuna in un
nodo di testo — verificato camminando il DOM, non cercando nella sorgente.

---

# Giro 18 — il file sbagliato, 24 agosto 2026

Nessuno aveva mai provato a caricare **il file sbagliato**. È il momento in cui
un tool o ti aiuta o ti fa perdere mezz'ora: hai il template davanti, hai
sbagliato foglio, e il messaggio ti dice dove guardare — oppure no.

I file di prova li ho costruiti dentro la pagina: una cartella con un foglio che
non c'entra, una con i fogli giusti e le intestazioni sbagliate, un PDF
rinominato `.xlsx`, otto byte a caso.

## `XLSX.read` non si lamenta di niente

È il difetto peggiore del giro, ed è condiviso da tutti e sei i tool.

Su **otto byte a caso** la libreria non solleva nulla: restituisce una cartella
con un foglio chiamato «Sheet1». Su un file di testo, uguale. Quindi ogni tool,
davanti a un PDF rinominato o a un download corrotto, rispondeva:

> Fogli obbligatori mancanti: Stato Patrimoniale, Conto Economico.

Chi legge apre il file, cerca i fogli, non li trova — e non capisce perché,
visto che il file «è» un Excel. Non lo è.

Corretto nella **guardia condivisa**, avvolgendo `XLSX.read` come già si avvolge
`XLSX.write`. Passano le cartelle `.xlsx` (che sono zip), i vecchi `.xls` e il
testo — due input accettano anche il CSV — e si ferma quello che non è né l'uno
né l'altro:

> Il file non è una cartella Excel: non ne ha il formato. Controlla di aver
> scelto il file giusto — un PDF o un documento rinominato .xlsx non basta —
> oppure riscarica il template.

Verificato in pagina su tutti e quattro i casi. Una cartella vera senza i fogli
richiesti continua a dire, giustamente, **quali** fogli mancano.

## Il pannello teneva gli errori del file precedente

In Analisi: si carica un file senza i fogli richiesti — quattro errori — e poi
un secondo file, con i fogli giusti e le intestazioni sbagliate — dieci errori.
Il pannello ne mostrava **quattordici**, e fra questi «Foglio "Stato
Patrimoniale" assente o vuoto», riferito al file di prima mentre nel file nuovo
quel foglio c'è.

`v20Reject` accodava invece di sostituire. Ora dopo il secondo import si vedono
solo i suoi dodici.

## Quello che ho verificato e NON era un difetto

- **L'accumulo dei centri di costo** in Analisi è voluto: filtra i vecchi
  messaggi `Centri:` e aggiunge i nuovi, perché quel file si carica *sopra* una
  sessione già aperta. Sostituire, lì, perderebbe le diagnosi del bilancio.
- **Il Bilancio sembrava muto** davanti a un file con le intestazioni sbagliate.
  Non lo è: dice «Intestazioni non valide nel foglio "Stato Patrimoniale". Sono
  obbligatorie: Codice conto, Descrizione conto, Importo esercizio corrente e
  Voce IV Direttiva» — cioè esattamente quello che serve. L'avevo letto **dopo**
  che il toast era sparito. I messaggi che si autodistruggono vanno raccolti
  mentre compaiono, con un osservatore, non cercati dopo.
- **Analisi rifiuta e conserva**: «Importazione rifiutata: 4 errori. La sessione
  precedente non è stata modificata». Il lavoro in corso non si perde, ed è
  scritto.

---

# Giro 19 — il peso, misurato, 24 agosto 2026

Le pagine dei tool pesano fra 1,2 e 2,4 MB su disco. Nessuno aveva mai misurato
cosa vuol dire davvero, né cosa c'è dentro.

## Cosa c'è dentro

| tool | totale | SheetJS | jsPDF | codice del tool | HTML+CSS |
|---|---|---|---|---|---|
| F24 | 2.361 K | 929 K | 410 K | 910 K | 110 K |
| LIPE | 1.995 K | 929 K | 442 K | 558 K | 65 K |
| Bilancio | 1.804 K | 929 K | 442 K | 352 K | 79 K |
| Analisi | 1.685 K | 929 K | — | 676 K | 79 K |
| Fascicolo | 1.332 K | 929 K | — | 343 K | 59 K |
| Confronto regimi | 1.212 K | 929 K | — | 213 K | 69 K |

**SheetJS occupa 929 K in ognuno dei sei**: 5,4 MB degli stessi byte ripetuti
sul sito, e in ogni pagina è il pezzo più grande.

## Quanto viaggia davvero

Misurato **sul sito pubblicato**, non in locale: Cloudflare risponde con
`Content-Encoding: br`.

| tool | in chiaro | compresso |
|---|---|---|
| F24 | 2.354 K | **961 K** |
| LIPE | 1.993 K | **761 K** |
| Bilancio | 1.799 K | **534 K** |
| Analisi | 1.682 K | **474 K** |
| Fascicolo | 1.328 K | **416 K** |
| Confronto regimi | 1.212 K | **373 K** |

Da 2,3 MB a 961 K è un fattore 2,4. Resta parecchio per un telefono in 4G, ma
non è la cifra da spavento che suggeriva il peso su disco.

## Quello che NON propongo, e perché

L'ottimizzazione ovvia sarebbe estrarre SheetJS in un file condiviso: −929 K per
pagina, una copia sola in cache per tutto il sito. **Non la propongo**, ed è
scritto in `CONTESTO.md` da prima di questo audit:

> Separare bundle e librerie e caricare i moduli su richiesta contraddice
> l'architettura voluta (file unico, elaborazione locale, apribile da disco).

È una decisione già presa, con una ragione: un tool che sta in un file solo si
apre anche da disco, senza server. Il costo di quella scelta è quantificato qui
sopra — 5,4 MB di byte ripetuti — e va tenuto d'occhio se i tool diventano
dieci. Ma non è un difetto: è un prezzo scelto.

## La promessa mantenuta

«L'elaborazione dei file avviene localmente nel browser» sta nel piede di ogni
pagina. Misurato aprendo i sei tool e leggendo le risorse caricate:
**sette risorse ciascuno, zero fuori origine**. Nessun CDN, nessun font remoto,
nessuna misurazione di traffico. La promessa regge.

Ora la tiene `tests/niente-rete.test.mjs`: nessun tool può caricare uno script,
un foglio di stile, un carattere o un'immagine da un altro dominio, né fare
`fetch`, `WebSocket` o `sendBeacon` verso un indirizzo assoluto. Gli indirizzi
che **dichiarano** e non caricano — `canonical`, `og:image`, gli `hreflang` —
restano liberi: servono ai motori di ricerca e non fanno partire richieste.
Provato aggiungendo a LIPE uno script da un CDN finto: fallisce e lo nomina.

## Quello che qui non si può misurare

Il tempo di primo disegno. Il riquadro del browser di questa sessione non
compone, quindi le voci `paint` non arrivano mai e `domInteractive` coincide con
la fine della risposta: numeri che direbbero solo quanto è veloce il disco. La
compilazione del JavaScript, che invece si misura, costa **fra 0 e 18 ms** su
tutti e sei — V8 compila pigramente, e non è lì il collo di bottiglia.

**158 test.**

---

# Giro 20 — l'altezza che si vede, 24 agosto 2026

Segnalato da Riccardo dal telefono: **il menu laterale resta fisso e non scorre,
e i pulsanti più in basso non si riescono a premere perché non si vedono.**

I tre sintomi hanno una causa sola, e non è il menu.

## `100vh` non è l'altezza che si vede

Su iOS e Android `100vh` è l'altezza **grande**: quella che la finestra avrebbe
se la barra del browser sparisse. La barra invece c'è, e occupa fra 60 e 110
punti. Un elemento `position: fixed` alto `100vh` finisce quindi sotto di essa —
e se è un contenitore che scorre, **non basta scorrerlo fino in fondo**: misura
sé stesso più alto di quanto si vede, quindi gli ultimi cento punti non entrano
mai in vista. È esattamente la sensazione di «resta fisso e non scorre».

Misurato a 375×812 su Analisi:

- menu laterale `position: fixed; height: 100vh` → si crede alto 812;
- contenuto del menu: **1.415 punti**, 22 voci;
- l'ultima, «Come si usa», a 1.310.

Lo stesso `100vh` sta su `.app-main` in tutti e sei i tool, e i tetti dei
dialoghi sono `max-height: 90vh` (Analisi) o `calc(100vh − 81)` (F24): un
riquadro alto 730 su uno schermo che ne mostra 700 tiene i pulsanti — che stanno
in fondo — sotto la barra, dove non si arriva.

## La correzione

Tutta in `assets/tal-app.css`, sotto i 980 px, quindi **una volta per tutti e
sei i tool**:

1. **`height: 100dvh`** sul menu fissato e **`min-height: 100dvh`** sull'area
   principale. `dvh` è l'altezza *dinamica*: segue la finestra vera. La riga con
   `vh` resta prima, come ripiego per i browser che non conoscono `dvh`.
2. **Spazio sotto l'ultima voce del menu**, safe area compresa: senza, l'ultima
   voce tocca il bordo.
3. **Nei dialoghi a scorrere è l'involucro, non il riquadro**, e i tetti in `vh`
   sul riquadro vengono neutralizzati. Un dialogo più alto dello schermo scorre
   tutto, pulsanti compresi, e comincia in alto invece che centrato — centrato,
   sborda da tutte e due le parti.

Verificato a 375×812: scorrendo il menu fino in fondo, «Come si usa» arriva a
704–748, **dentro** la finestra. E la finestra «Nuova anagrafica» di F24, che è
alta **1.333 punti**, ora scorre nell'involucro: «Salva anagrafica» passa da
1.331 a 774. Armatura responsive: **42 su 42**.

## Perché l'armatura non l'aveva visto, e non poteva

L'emulatore **non ha la barra del browser**: lì `vh` e `dvh` valgono lo stesso
numero, e il difetto non esiste. Le 42 misure del responsive non potevano
trovarlo, e infatti «il mobile su un telefono vero» era da giorni fra i punti
aperti di questo verbale — con scritto che l'armatura misura Chromium e non ha
`env(safe-area-inset-*)`.

Va detto fino in fondo: **anche adesso non posso mostrare il prima che fallisce**,
perché qui il prima e il dopo danno lo stesso numero. Quello che posso mostrare è
che la regola vince nella cascata, che il browser supporta `dvh`, e che un
dialogo più alto dello schermo ora si scorre per intero. La prova sul campo è il
tuo telefono.

`tests/altezza-telefono.test.mjs` tiene le tre regole: se qualcuno toglie il
`dvh`, la safe area sotto il menu o lo scorrimento dell'involucro, fallisce e
dice quale. Provato togliendo `height:100dvh`. **162 test.**

---

# Giro 21 — LIPE in tre viste, e la voce, 24 agosto 2026

Due richieste di Riccardo: la prima pagina di LIPE è troppo compressa, e va
ricontrollato che da nessuna parte ci siano frasi in prima persona che suonano
come un assistente.

## La configurazione di LIPE era una pagina sola

«Clienti» conteneva quattro cose che non si guardano nello stesso momento:

| pannello | altezza a 1280 | a 375 |
|---|---|---|
| Anagrafica cliente | 512 | 1.159 |
| Dati telematici XML IVP18 | 691 | 1.492 |
| Codiciario IVA | 395 | 715 |
| Archivio clienti | 149 | 170 |
| **la vista intera** | **2.185** | **4.158** |

Due schermate e mezza su desktop, cinque sul telefono, per arrivare in fondo a
una configurazione — con il codiciario in mezzo, che si tocca una volta ogni
tanto, fra due cose che si toccano sempre.

**Il meccanismo delle viste in LIPE esisteva già** (`data-lipeview`): la
configurazione semplicemente non lo usava. Adesso sono tre voci sotto
«Configurazione», nell'ordine in cui si fanno le cose la prima volta:

- **Clienti** — anagrafica e archivio: 1.072 px (1,2 schermate)
- **Dati telematici** — intestazione dell'XML e impegno: 742 px (0,8)
- **Codiciario IVA** — il significato stabile dei codici: 426 px (0,5)

Verificato che il tool funziona ancora da capo a fondo dopo lo spostamento:
l'esempio carica, il campo del trasmittente — che ora sta in un'altra vista — si
compila, il codiciario ha le sue sette righe, il salvataggio dice «7 codici IVA»
e l'XML esce a 2.440 byte. Armatura responsive: 42 su 42.

**Un errore di misura, corretto per tempo.** La prima misura diceva 10.003 px.
Era falsa: il riquadro del browser era largo zero, la griglia dei campi
collassava e ogni campo diventava una colonna. Le altezze vere sono quelle della
tabella qui sopra, prese a 1280 e a 375 con la finestra dichiarata. Quando una
misura sembra enorme, la prima cosa da guardare è `innerWidth`.

## La voce: due frasi in tutto il sito

Cercate su **75 pagine** le formule che tradiscono un assistente che parla — «non
ho trovato», «ho analizzato», «posso aiutarti», «mi dispiace», «ti consiglio»,
«sono un assistente». Ne restavano **due**, tutte e due in Confronto regimi:

> Per CNPADC **non ho trovato** gli importi ridotti in una fonte citabile…

Dicono una cosa che serve — che la riduzione dei minimi CNPADC non è modellata
perché manca una fonte citabile — quindi cambia la voce, non il contenuto:

> Per la CNPADC gli importi ridotti **non risultano** in una fonte citabile…

**Quello che ho lasciato, e perché.** I prompt di «Configura con AI» dicono
«Agisci come assistente di data preparation»: parlano a un'AI, è il loro
mestiere. «Ciao, sono Riccardo… Ho creato Tax Automation Lab» è Riccardo che
parla. «Ho capito: controllerò il file con il software dell'Agenzia» in F24 lo
dice **l'utente**, spuntando una casella. E i commenti nel codice sono in prima
persona in tutto il repository: sono note a chi mantiene, non scritte di
prodotto.

`tests/voce.test.mjs` tiene la regola su tutte le pagine, con le eccezioni
scritte. Provato aggiungendo a LIPE la frase del brief — «Non ho trovato fonti
aggiornate per questo codice» — e fallisce nominandola. **169 test.**

LIPE passa a 3.6.3.

---

## Ancora da guardare

- decidere se passare a una build di SheetJS che scrive gli stili: oggi i colori
  per tematica si possono dare nel PDF ma non nelle cartelle Excel, e tredici
  «!freeze» più dodici stili di cella restano dichiarati e invisibili;
- il mobile su un telefono vero. Il giro 20 ha corretto l’altezza dinamica e lo
  scorrimento dei dialoghi, ma l’emulatore non ha la barra del browser: il prima
  e il dopo danno lo stesso numero. Serve una prova sul telefono;
- il modulo di controllo dell’Agenzia per il tracciato F24A0 e per il file
  LIPE. Il programma è estratto e pronto in `C:\Users\RiccardoZedda\
  DesktopTelematico`, e i due file da controllare stanno in `da-controllare`:
  restano da installare i moduli dall’applicazione e farli girare. La prima
  domanda da porgli è la lunghezza del record F24A0.
