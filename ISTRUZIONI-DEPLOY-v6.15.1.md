# Deploy v6.15.1

Overlay incrementale: copiare il contenuto sopra il repository corrente, mantenendo `.git` e tutti gli altri file. Non creare una GitHub Release. Dopo il push attendere GitHub Pages e svuotare la cache Cloudflare.
